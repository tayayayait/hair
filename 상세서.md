# 아이디헤어 신규 고객 디지털 설문 시스템 상세 명세서

| 문서 항목 | 내용 |
|---|---|
| 문서 ID | IDHAIR-SURVEY-SPEC-001 |
| 문서 버전 | v1.1 |
| 작성일 | 2026-08-03 |
| 대상 | 기획자, UI/UX 디자이너, 프론트엔드·백엔드 개발자, QA, 운영 담당자 |
| 기준 환경 | 고객 설문: 태블릿 우선, 모바일·PC 지원 / 관리자: PC 우선, 태블릿 지원 |
| 기준 시간대 | Asia/Seoul(KST, UTC+9) |
| 접근성 목표 | WCAG 2.2 AA 준수 |
| 원본 자료 | `KakaoTalk_Photo_2026-07-30-16-24-15.jpeg` 종이 설문지 및 2026-08-03 사용자 제공 확정 전사본 |

> 이 문서는 별도 해석 없이 화면 설계와 개발 기준으로 사용할 수 있는 기능·UI/UX 명세이다. 본문에서 **필수**는 반드시 구현해야 하는 항목, **권장**은 특별한 사유가 없는 한 적용하는 항목, **금지**는 구현하면 안 되는 항목을 뜻한다.

## 변경 이력

| 버전 | 일자 | 변경 내용 |
|---|---|---|
| v1.0 | 2026-08-03 | 최초 상세 명세 작성 |
| v1.1 | 2026-08-03 | 사용자 제공 확정 전사본 기준 표시 문구 교정, 종이 카드 개인정보 동의 원문과 디지털 적용 규칙 추가 |

## 목차

1. [문서 적용 원칙](#0-문서-적용-원칙)
2. [프로젝트 개요](#1-프로젝트-개요)
3. [사용자 역할 및 권한](#2-사용자-역할-및-권한)
4. [정보 구조 및 내비게이션](#3-정보-구조-및-내비게이션)
5. [핵심 사용자 흐름](#4-핵심-사용자-흐름)
6. [데이터 및 설문 구조](#5-데이터-및-설문-구조)
7. [화면 목록](#6-화면-목록)
8. [화면별 상세 명세](#7-화면별-상세-명세)
9. [디자인 시스템](#8-디자인-시스템)
10. [공통 컴포넌트 규격](#9-공통-컴포넌트-규격)
11. [반응형 상세 기준](#10-반응형-상세-기준)
12. [접근성 원칙](#11-접근성-원칙)
13. [상태값 정의](#12-상태값-정의)
14. [대시보드·필터·집계](#13-대시보드필터집계-상세-규칙)
15. [XLSX 내보내기](#14-xlsx-내보내기-명세)
16. [백업·복구](#15-백업복구-상세-명세)
17. [개인정보·보안](#16-개인정보보안-기준)
18. [예외 처리](#17-예외-처리-기준)
19. [데이터 모델](#18-데이터-모델-및-관계)
20. [성능·운영 품질](#19-성능운영-품질-기준)
21. [QA 및 인수 기준](#20-qa-및-인수-기준)
22. [개발·디자인 산출물](#21-개발디자인-산출물-기준)
23. [출시 전 체크리스트](#22-출시-전-필수-결정-및-체크리스트)
24. [참조 기준](#23-참조-기준)

---

## 0. 문서 적용 원칙

### 0.1 우선순위

요구사항이 충돌할 때 다음 순서로 판단한다.

1. 개인정보 보호 및 서버 권한 제어
2. 고객이 설문을 안전하게 제출하는 핵심 흐름
3. 설문 원본 데이터와 이력의 보존
4. 태블릿 터치 사용성 및 접근성
5. 관리자 업무 효율
6. 시각적 표현

### 0.2 확정 기준과 정책 결정 항목

- 본 문서의 화면 구조, 컴포넌트 수치, 반응형 규칙, 상태값, 권한 범위는 v1.0 구현 기준으로 확정한다.
- 개인정보 보유기간, 파기 주기, 개인정보 처리 위탁사, 선택적 마케팅 수신 여부는 법무·운영 미팅 후 확정한다.
- 정책 미확정 항목은 `[POLICY-TBD-nn]`으로 표시한다. 개발자는 해당 값을 하드코딩하지 않고 슈퍼관리자 설정 또는 환경 설정값으로 구현한다.
- 정책 미확정 상태에서는 운영 배포를 막되, 개발·스테이징 환경에서는 테스트 값으로 동작할 수 있어야 한다.

### 0.3 기본 가정

- 약 100개 매장을 하나의 본사 조직(tenant) 아래에서 운영한다.
- 기존 CRM·기존 고객 DB와 API, 파일, 수동 업로드를 포함한 모든 연동은 v1.0 범위에서 제외한다.
- 고객은 별도 계정을 만들거나 로그인하지 않는다.
- 고객 설문은 매장 직원이 담당 디자이너를 지정한 키오스크 세션으로 시작한다.
- 설문 양식 커스터마이즈 권한은 v1.0에서 **슈퍼관리자만** 가진다. 매장별 설문 분기는 지원하지 않는다.
- 설문에 스타일 사진을 업로드하는 기능은 v1.0 범위에서 제외한다. 원본 양식의 “원하는 스타일 사진”은 사진 보유 여부만 묻는다.
- 운영 언어는 한국어이다. 원본 종이 설문지에 포함된 영문 보조 문구는 디지털 화면에서 기본 노출하지 않는다.

---

## 1. 프로젝트 개요

### 1.1 목적

아이디헤어 매장에서 사용하는 종이 신규 고객 설문지를 디지털 설문으로 전환하고, 설문으로 수집한 고객·상담 데이터를 본사, 매장, 담당 디자이너 권한에 따라 조회·분석·내보내기·백업할 수 있는 웹 기반 시스템을 구축한다.

### 1.2 핵심 목표

- 고객이 태블릿에서 5분 이내에 설문을 완료할 수 있어야 한다.
- 설문 항목 변경 후에도 과거 응답의 질문과 답변 의미가 변하지 않아야 한다.
- 본사·매장·디자이너의 조회 범위가 화면뿐 아니라 모든 API에서 강제되어야 한다.
- 대시보드의 합계, 목록, 차트, 엑셀 내보내기 결과가 동일한 필터 기준으로 일치해야 한다.
- 개인정보 동의 문구, 동의 버전, 동의 시각, 동의 주체를 응답과 함께 보관해야 한다.
- 운영자가 실수로 데이터를 삭제하거나 양식을 변경해도 이력과 백업을 통해 추적할 수 있어야 한다.

### 1.3 포함 범위

- 신규 고객 설문 작성, 임시 저장, 검토, 제출
- 고객용 태블릿·모바일·PC 반응형 화면
- 설문 템플릿/버전/필드/선택지 편집 및 게시
- 본사·매장·디자이너 3단계 계정과 권한
- 매장·사용자·담당 디자이너 관리
- 고객 및 설문 응답 목록·상세 조회
- 기간·매장·디자이너·질문·선택지 필터
- Chart.js 기반 KPI 및 차트
- XLSX 내보내기와 작업 이력
- 전체 데이터 백업, 무결성 검사, 복구
- 감사 로그와 주요 개인정보 접근 기록
- 개인정보 수집·이용 동의 및 만 14세 미만 법정대리인 동의 분기

### 1.4 제외 범위

- 기존 CRM, 예약, 결제, POS, 멤버십, 카카오 알림톡 연동
- 고객 회원가입·로그인
- 온라인 예약, 시술 기록, 결제 금액 관리
- 사진·동영상 업로드
- 매장별 서로 다른 설문 템플릿
- 마케팅 문자·이메일 발송
- AI 추천, 고객 등급 자동 산정
- 오프라인 상태에서의 신규 설문 제출
- 원본 응답의 물리 삭제를 디자이너·원장에게 허용하는 기능

### 1.5 용어 정의

| 용어 | 정의 |
|---|---|
| 조직 | 아이디헤어 본사 단위. v1.0에서는 1개 조직을 전제로 한다. |
| 매장 | 설문 데이터의 1차 소유 범위. 모든 고객·응답은 정확히 한 매장에 속한다. |
| 담당 디자이너 | 해당 설문 상담을 담당한 사용자. 제출 시점의 사용자 ID와 표시명을 스냅샷으로 보관한다. |
| 고객 | 설문을 제출한 사람의 기본 식별정보 레코드. 한 고객은 여러 설문 응답을 가질 수 있다. |
| 설문 템플릿 | 질문 구조의 논리적 묶음. |
| 설문 버전 | 특정 시점에 게시된 변경 불가능한 질문·선택지 스냅샷. |
| 응답 | 고객 한 명이 특정 설문 버전으로 제출한 1건의 데이터. |
| 키오스크 세션 | 매장과 담당 디자이너가 고정된 고객용 설문 실행 세션. 관리자 세션과 분리한다. |
| 무효 처리 | 통계와 기본 목록에서 제외하되 감사 및 복구 목적의 원본은 보존하는 논리 상태 변경. |
| 파기 | 확정된 보유기간 또는 정보주체 요청에 따라 식별정보를 되돌릴 수 없게 제거하는 처리. |

---

## 2. 사용자 역할 및 권한

### 2.1 역할 코드

| 표시명 | 코드 | 데이터 범위 |
|---|---|---|
| 슈퍼관리자(본사) | `SUPER_ADMIN` | 전체 매장, 전체 사용자, 전체 고객·응답 |
| 원장(매장 관리자) | `STORE_OWNER` | 본인이 현재 소속된 1개 매장의 고객·응답 및 디자이너 |
| 디자이너 | `DESIGNER` | 현재 소속 매장에서 본인에게 배정된 응답과 연결 고객 |

### 2.2 기능 권한 매트릭스

| 기능 | 슈퍼관리자 | 원장 | 디자이너 |
|---|:---:|:---:|:---:|
| 전체 대시보드 | 전체 | 소속 매장 | 본인 데이터 |
| 설문 응답 목록·상세 | 전체 | 소속 매장 전체 | 본인 담당 응답 |
| 고객 목록·상세 | 전체 | 소속 매장 전체 | 본인 담당 응답과 연결된 고객 |
| 개인정보 전체값 열람 | 가능, 기록 필수 | 가능, 기록 필수 | 본인 담당 고객만 가능, 기록 필수 |
| 고객 기본정보 정정 | 가능 | 소속 매장만 가능 | 불가 |
| 응답 무효 처리 | 가능 | 소속 매장만 가능 | 불가 |
| XLSX 내보내기 | 전체 범위 | 소속 매장 | 본인 범위 |
| 미마스킹 개인정보 포함 내보내기 | 가능, 사유 필수 | 가능, 사유 필수 | 불가 |
| 설문 양식 편집·게시 | 가능 | 불가 | 불가 |
| 매장 생성·수정·비활성화 | 가능 | 불가 | 불가 |
| 원장 계정 관리 | 가능 | 불가 | 불가 |
| 디자이너 초대·정지 | 전체 | 소속 매장만 | 불가 |
| 사용자 매장 이동 | 가능 | 불가 | 불가 |
| 키오스크 세션 시작 | 가능 | 가능 | 가능 |
| 전체 백업·복구 | 가능 | 불가 | 불가 |
| 감사 로그 | 전체 | 소속 매장 범위 | 본인 중요 작업만 |
| 개인정보 보유·파기 정책 설정 | 가능 | 조회만 | 불가 |

### 2.3 서버 권한 규칙

화면에서 메뉴를 숨기는 것만으로 권한을 처리하면 안 된다. 모든 조회·수정·내보내기 API는 인증 사용자 기준으로 다음 조건을 서버에서 적용한다.

```text
SUPER_ADMIN:
  organization_id = current_user.organization_id

STORE_OWNER:
  organization_id = current_user.organization_id
  AND store_id = current_user.store_id

DESIGNER:
  organization_id = current_user.organization_id
  AND store_id = current_user.store_id
  AND designer_user_id = current_user.id
```

- 클라이언트가 `store_id`, `designer_user_id`를 임의로 보내도 서버가 허용 범위를 다시 계산한다.
- 권한 밖 ID를 직접 URL에 입력한 경우 데이터 존재 여부를 노출하지 않고 `404 NOT_FOUND`로 응답한다.
- 디자이너가 다른 매장으로 이동하면 이전 매장 응답 접근 권한은 즉시 종료한다. 이전 응답의 담당자 스냅샷은 유지하고 이전 매장 원장과 본사는 계속 열람한다.
- 비활성 사용자·매장은 신규 키오스크 세션과 로그인에 사용할 수 없다.
- 복구 작업 중에는 읽기 전용 모드로 전환하고 모든 쓰기 API는 `503 MAINTENANCE_MODE`를 반환한다.

### 2.4 개인정보 표시 규칙

- 목록 화면 기본값: 이름 `김*희`, 연락처 `010-1234-****`, 생년월일 `1990-**-**`.
- 상세 화면: 권한이 있는 사용자가 “전체 정보 보기”를 선택하면 전체값을 표시한다.
- 전체값 조회 시 `actor_id`, `target_customer_id`, `screen_id`, `viewed_fields`, `viewed_at`, `ip`, `user_agent`를 감사 로그에 기록한다.
- 디자이너는 XLSX에 전체 개인정보를 포함할 수 없다.
- 슈퍼관리자와 원장은 내보내기 모달에서 “개인정보 전체값 포함”을 선택하고 10자 이상의 사용 사유를 입력해야 한다.

---

## 3. 정보 구조 및 내비게이션

### 3.1 고객용 정보 구조

```text
키오스크 준비
└─ 시작 안내·개인정보 동의
   ├─ 동의하지 않음 → 종료·초기화
   ├─ 만 14세 미만 → 법정대리인 동의
   └─ 동의 완료
      └─ 1. 기본 정보
         └─ 2. 방문·상담 정보
            └─ 3. 모발·두피 정보
               └─ 4. 검토
                  └─ 제출 완료 → 15초 후 초기화
```

### 3.2 관리자 정보 구조

```text
로그인
├─ 대시보드
├─ 설문 응답
│  ├─ 응답 목록
│  └─ 응답 상세
├─ 고객
│  ├─ 고객 목록
│  └─ 고객 상세
├─ 설문 관리 [슈퍼관리자]
│  ├─ 템플릿·버전 목록
│  ├─ 설문 편집
│  └─ 미리보기·게시
├─ 매장 관리 [슈퍼관리자]
├─ 사용자 관리 [슈퍼관리자/원장]
├─ 내보내기
├─ 백업·복구 [슈퍼관리자]
├─ 감사 로그
└─ 내 계정
```

### 3.3 역할별 주 내비게이션

| 메뉴 | 슈퍼관리자 | 원장 | 디자이너 |
|---|:---:|:---:|:---:|
| 대시보드 | 노출 | 노출 | 노출 |
| 설문 응답 | 노출 | 노출 | 노출 |
| 고객 | 노출 | 노출 | 노출 |
| 설문 관리 | 노출 | 숨김 | 숨김 |
| 매장 관리 | 노출 | 숨김 | 숨김 |
| 사용자 관리 | 노출 | 노출 | 숨김 |
| 내보내기 | 노출 | 노출 | 노출 |
| 백업·복구 | 노출 | 숨김 | 숨김 |
| 감사 로그 | 노출 | 노출 | “내 활동”으로 노출 |
| 내 계정 | 노출 | 노출 | 노출 |

---

## 4. 핵심 사용자 흐름

### 4.1 최초 운영 설정

1. 슈퍼관리자가 본사 계정으로 로그인한다.
2. 매장을 생성하고 매장 코드를 지정한다.
3. 원장 계정을 초대하고 매장에 연결한다.
4. 원장 또는 슈퍼관리자가 디자이너를 초대한다.
5. 슈퍼관리자가 개인정보 보유기간과 동의 문구를 설정한다.
6. 슈퍼관리자가 초기 설문 초안을 검토하고 게시한다.
7. 매장 직원이 키오스크 세션을 시작한다.

### 4.2 고객 설문 제출

1. 직원이 로그인 후 “설문 시작”을 누른다.
2. 매장과 담당 디자이너를 확인하고 키오스크 세션을 연다.
3. 직원이 태블릿을 고객에게 전달한다.
4. 고객이 안내와 개인정보 수집·이용 내용을 확인한다.
5. 고객이 만 14세 이상 여부와 필수 동의 여부를 선택한다.
6. 고객이 기본 정보와 상담 문항을 입력한다.
7. 각 단계 이동 시 서버에 임시 저장한다.
8. 검토 화면에서 입력 내용을 확인하고 제출한다.
9. 서버가 중복 제출 방지 키를 확인한 뒤 고객과 응답을 하나의 트랜잭션으로 저장한다.
10. 완료 화면에는 개인정보를 표시하지 않는다.
11. 15초 후 또는 “다음 고객 설문”을 누르면 모든 입력과 임시 토큰을 제거하고 동의 화면으로 돌아간다.

### 4.3 설문 양식 변경

1. 슈퍼관리자가 현재 게시 버전에서 “새 초안 만들기”를 선택한다.
2. 시스템이 게시 버전을 복제해 `DRAFT` 버전을 만든다.
3. 관리자가 필드·선택지·필수 여부·순서를 변경한다.
4. 미리보기에서 모바일·태블릿·PC 레이아웃을 확인한다.
5. 게시 전 검증에서 오류와 과거 버전 대비 변경 내용을 확인한다.
6. 게시하면 초안이 새 `PUBLISHED` 버전이 되고 이전 버전은 `ARCHIVED`가 된다.
7. 이미 시작한 설문은 시작 당시 버전으로 끝까지 진행한다.
8. 새로 시작한 설문부터 새 버전을 사용한다.

### 4.4 조회·필터·내보내기

1. 관리자가 대시보드 또는 응답 목록에서 기간과 조건을 선택한다.
2. 화면 합계, 차트, 목록에 동일한 필터 객체를 적용한다.
3. “엑셀 다운로드”를 누르면 현재 필터와 예상 행 수를 모달에 표시한다.
4. 개인정보 포함 범위를 선택하고 필요한 경우 사유를 입력한다.
5. 50,000행 이하이면 즉시 파일을 생성하고, 초과하면 백그라운드 작업으로 전환한다.
6. 완료된 파일은 내보내기 이력에서 권한이 있는 사용자만 제한 시간 동안 다운로드한다.

### 4.5 백업·복구

1. 슈퍼관리자가 백업을 생성한다.
2. 서버가 암호화 스냅샷과 체크섬을 생성한다.
3. 복구 시 슈퍼관리자가 대상 백업을 선택하고 영향 요약을 확인한다.
4. 최근 전체 백업을 자동으로 한 번 더 만든다.
5. 확인 문구와 현재 비밀번호를 입력한다.
6. 시스템을 읽기 전용으로 전환하고 무결성 검사를 통과한 백업만 복구한다.
7. 성공 시 읽기 전용을 해제하고 감사 로그에 기록한다.
8. 실패 시 복구 직전 백업으로 자동 롤백하고 오류 코드를 제공한다.

---

## 5. 데이터 및 설문 구조

### 5.1 공통 식별자와 저장 형식

| 항목 | 규칙 |
|---|---|
| 내부 ID | UUID v4 또는 동등한 128비트 랜덤 ID. 순차 ID를 외부 URL에 노출하지 않는다. |
| 날짜·시각 저장 | DB에는 UTC ISO 8601로 저장하고 화면·XLSX에는 KST로 표시한다. |
| 전화번호 | 휴대전화번호의 숫자만 정규화해 `01012345678` 형태로 저장하고 화면에서 `010-1234-5678`처럼 표현한다. |
| 생년월일 | `YYYY-MM-DD`. 시각 정보를 붙이지 않는다. |
| 선택지 | 화면 라벨과 별도로 변경 불가능한 `option_code`를 저장한다. |
| 질문 | 변경 불가능한 `field_key`와 버전별 라벨 스냅샷을 함께 저장한다. |
| 주소 | 자유 입력 문자열, 최대 200자. 주소 검색 API는 v1.0 범위에서 제외한다. |
| 사용자 표시명 | 응답 제출 시점의 이름을 별도 스냅샷으로 보관한다. |

### 5.2 지원 필드 유형

| `field_type` | UI | 주요 설정 | 저장값 |
|---|---|---|---|
| `SHORT_TEXT` | 한 줄 입력 | 최소/최대 길이, 정규식 | 문자열 |
| `LONG_TEXT` | 여러 줄 입력 | 최대 길이 | 문자열 |
| `PHONE` | 전화번호 입력 | 국가코드, 허용 패턴 | 정규화 문자열 |
| `DATE` | 날짜 선택 | 최소/최대 날짜 | `YYYY-MM-DD` |
| `SINGLE_CHOICE` | 라디오/선택 카드 | 선택지, 기본값 | `option_code` |
| `MULTI_CHOICE` | 체크박스/선택 카드 | 선택지, 최소/최대 선택, 배타 옵션 | `option_code[]` |
| `SELECT` | 드롭다운 | 선택지, 검색 허용 | `option_code` |
| `CONSENT` | 동의 카드+체크박스 | 문구 버전, 필수 여부 | 동의 스냅샷 ID |
| `SECTION_TITLE` | 제목·설명 | 제목, 설명 | 저장 안 함 |
| `DISPLAY_TEXT` | 안내문 | 본문 | 저장 안 함 |

- `FILE`, `IMAGE`, `SIGNATURE`, `RATING`, `NUMBER` 필드는 v1.0 편집기에서 제공하지 않는다.
- 조건부 노출은 동일 설문 내 앞선 필드의 단일 선택 또는 다중 선택 포함 여부만 지원한다.
- 조건은 `equals`, `includes` 두 연산자만 지원한다.
- 숨겨진 필드의 기존 입력값은 즉시 삭제하고 제출 데이터에 포함하지 않는다.

### 5.3 초기 설문 필드 정의

#### 5.3.1 시스템·동의 영역

| 순서 | `field_key` | 표시 라벨 | 유형 | 필수 | 규칙 |
|---:|---|---|---|:---:|---|
| 1 | `assigned_designer_id` | 담당 디자이너 | 시스템값 | O | 키오스크 시작 시 직원이 지정. 고객 수정 불가. |
| 2 | `age_14_or_over` | 만 14세 이상입니다 | `SINGLE_CHOICE` | O | `YES`/`NO`. `NO`이면 법정대리인 동의 영역 노출. |
| 3 | `privacy_consent` | 개인정보 수집·이용 동의 | `CONSENT` | O | 동의하지 않으면 개인정보 입력 및 제출 불가. |
| 4 | `guardian_name` | 법정대리인 성명 | `SHORT_TEXT` | 조건부 | `age_14_or_over=NO`일 때 필수, 2~50자. |
| 5 | `guardian_phone` | 법정대리인 연락처 | `PHONE` | 조건부 | `age_14_or_over=NO`일 때 필수. |
| 6 | `guardian_relationship` | 고객과의 관계 | `SHORT_TEXT` | 조건부 | `age_14_or_over=NO`일 때 필수, 2~30자. |
| 7 | `guardian_consent` | 법정대리인 개인정보 수집·이용 동의 | `CONSENT` | 조건부 | `age_14_or_over=NO`일 때 필수. |

#### 5.3.2 기본 정보

| 순서 | `field_key` | 표시 라벨 | 유형 | 필수 | 유효성·도움말 |
|---:|---|---|---|:---:|---|
| 10 | `customer_name` | 성함 | `SHORT_TEXT` | O | 2~50자, 숫자·이모지 단독 입력 금지. 예: 김아이 |
| 11 | `gender` | 성별 | `SINGLE_CHOICE` | X | `MALE` 남성(M), `FEMALE` 여성(W), `NO_ANSWER` 응답하지 않음 |
| 12 | `birth_date` | 생년월일 | `DATE` | X | 미래 날짜 금지, 오늘 기준 120년 이전 금지 |
| 13 | `phone` | 연락처 | `PHONE` | O | 국내 휴대전화 10~11자리, 입력 중 자동 하이픈 |
| 14 | `address` | 주소 | `SHORT_TEXT` | X | 최대 200자, “시/구/동까지만 입력해도 됩니다.” |
| 15 | `visit_source` | 방문동기 | `MULTI_CHOICE` | O | 최소 1개, 아래 선택지 사용 |
| 16 | `introducer_name` | 소개자 성함 | `SHORT_TEXT` | 조건부 | `visit_source`에 `INTRODUCTION` 포함 시 노출, 2~50자 |

`visit_source` 선택지:

| `option_code` | 라벨 |
|---|---|
| `NEARBY` | 거주지 근처 |
| `NAVER_SEARCH` | 네이버 검색 |
| `INTRODUCTION` | 지인소개 |
| `BLOG_INSTAGRAM` | 블로그/인스타그램 |
| `GOOGLE_SEARCH` | 구글 검색 |

#### 5.3.3 상담 선호 정보

| 순서 | `field_key` | 표시 라벨 | 유형 | 필수 | 선택 규칙 |
|---:|---|---|---|:---:|---|
| 20 | `style_photo_plan` | 원하는 스타일 사진 | `SINGLE_CHOICE` | X | 1개 선택 |
| 21 | `preferred_designer_level` | 시술담당 희망 직급 | `SINGLE_CHOICE` | X | 1개 선택, 도움말 `직급별 차등 금액` |
| 22 | `interested_services` | 관심 있는 메뉴 | `MULTI_CHOICE` | O | 최소 1개, `DECIDE_AFTER_CONSULTATION`은 배타 옵션 |
| 23 | `desired_image` | 원하는 이미지 | `MULTI_CHOICE` | O | 최소 1개, 최대 제한 없음 |
| 24 | `priority_points` | 가장 신경 써야 할 포인트 | `MULTI_CHOICE` | O | 최소 1개, 최대 제한 없음 |

선택지:

| 필드 | `option_code` → 라벨 |
|---|---|
| `style_photo_plan` | `HAS_PHOTO` → 유 / `NO_PHOTO` → 무 / `DESIGNER_RECOMMENDATION` → 상담 디자이너가 추천 |
| `preferred_designer_level` | `DIRECTOR` → 실장 / `SENIOR_CHIEF_DIRECTOR` → 선임수석실장 / `VICE_DIRECTOR` → 부원장 / `OWNER_DIRECTOR` → 원장 / `NO_PREFERENCE` → 관계없음 |
| `interested_services` | `STYLING` → 스타일링 / `CUT` → 컷 / `PERM` → 펌 / `COLOR` → 컬러 / `SCALP_CARE` → 두피관리 / `HAIR_CARE` → 모발케어 / `DECIDE_AFTER_CONSULTATION` → 상담 후 선택 |
| `desired_image` | `LUXURIOUS` → 고급스러운 / `NATURAL` → 자연스러운 / `UNIQUE` → 유니크한 / `YOUNGER` → 어려 보이는 / `REFINED` → 세련된 / `TRENDY` → 유행하는 / `PERSONALIZED` → 나에게 맞춤 추천 |
| `priority_points` | `DESIGN_CUT` → 디자인컷 / `HAIR_DAMAGE` → 모발손상 / `VOLUME_BANGS` → 볼륨 & 앞머리 / `EASY_MAINTENANCE` → 손질이 편한 / `FAST_SERVICE` → 신속진행 / `CURL_ELASTICITY` → 컬의 탄력 / `SENSITIVE_SCALP` → 예민한 두피 / `DETAILED_SERVICE` → 꼼꼼한 시술 |

#### 5.3.4 모발·두피·홈케어 정보

| 순서 | `field_key` | 표시 라벨 | 유형 | 필수 | 선택 규칙 |
|---:|---|---|---|:---:|---|
| 30 | `scalp_concerns` | 두피 고민 | `MULTI_CHOICE` | X | `NOT_SURE`는 배타 옵션 |
| 31 | `hair_concerns` | 모발 고민 | `MULTI_CHOICE` | X | 복수 선택 |
| 32 | `homecare_purchase_history` | 홈케어 구매 이력 | `MULTI_CHOICE` | X | `NO_INTEREST`는 배타 옵션 |

선택지:

| 필드 | `option_code` → 라벨 |
|---|---|
| `scalp_concerns` | `ITCHY` → 가려움 / `OILY` → 기름진 / `DRY` → 건조한 / `STINGING` → 따가운 / `SCALP_BUMPS` → 뾰루지 / `HAIR_LOSS` → 탈모 / `NOT_SURE` → 잘 모르겠음 |
| `hair_concerns` | `THINNING` → 얇아진 모발 / `AGING` → 에이징 모발 / `FRIZZY_DRY` → 부스스하고 건조한 모발 / `NO_VOLUME` → 볼륨이 없는 / `PARTIAL_CURL` → 부분적 곱슬모발 / `PREVIOUS_DAMAGE` → 이전 시술 후 손상된 모발 |
| `homecare_purchase_history` | `EXPERT_RECOMMENDED` → 전문가의 추천제품 / `SNS_POPULAR` → SNS 후기 인기제품 / `HOME_SHOPPING_BULK` → 홈쇼핑 대량구매 / `OFFLINE_STORE` → 오프라인 구매(마트, 백화점, 올리브영) / `NO_INTEREST` → 관심없음(잘 모름) |

> 위 라벨과 선택지는 2026-08-03 제공된 확정 전사본을 기준으로 한다. 디지털 화면에서도 띄어쓰기와 기호를 포함해 동일하게 표시한다.

### 5.4 필드 유효성 규칙

- 필수 표시는 라벨 뒤 빨간 별표만 쓰지 않고 `필수` 텍스트 배지를 함께 노출한다.
- 선택형 필드의 필수 오류: “1개 이상 선택해 주세요.”
- 전화번호 오류: “연락처를 휴대전화번호 10~11자리로 입력해 주세요.”
- 날짜 오류: “올바른 생년월일을 입력해 주세요.”
- 이름 오류: “성함을 2자 이상 입력해 주세요.”
- 오류는 필드 바로 아래에 표시하고 오류 요약을 단계 상단에 함께 제공한다.
- 오류 발생 시 첫 번째 오류 필드로 스크롤하고 포커스를 이동한다.
- 서버 검증 규칙은 프론트엔드 규칙과 동일하거나 더 엄격해야 한다.
- HTML, 스크립트, 제어문자는 서버에서 무해화한다. 원문을 그대로 HTML로 렌더링하지 않는다.

### 5.5 설문 버전 규칙

- `PUBLISHED` 버전은 질문, 선택지, 순서, 필수 여부를 수정하거나 삭제할 수 없다.
- 수정은 항상 게시 버전을 복제한 새 `DRAFT`에서 수행한다.
- 응답은 `template_version_id`, 필드 라벨, 선택지 라벨을 제출 당시 스냅샷으로 보관한다.
- 게시 버전은 동시에 하나만 활성화한다.
- 초안은 사용자별이 아니라 조직당 하나만 허용하며 편집 잠금과 충돌 감지를 적용한다.
- 편집 잠금은 마지막 활동 후 10분이며, 다른 관리자는 읽기 전용으로 열 수 있다.
- 게시 시 필수 검증:
  - 활성 섹션 1개 이상
  - 질문 필드 1개 이상
  - 모든 필드의 `field_key` 유일
  - 선택형 필드의 활성 선택지 2개 이상
  - 모든 조건식이 앞선 활성 필드를 참조
  - 필수 동의 필드 1개 이상
  - 순환 조건 없음

---

## 6. 화면 목록

| 화면 ID | 화면명 | 경로 예시 | 접근 역할 | 우선 기기 |
|---|---|---|---|---|
| `AUTH-001` | 관리자 로그인 | `/login` | 전체 관리자 | PC·태블릿 |
| `KSK-001` | 키오스크 세션 설정 | `/admin/kiosk` | 전체 관리자 | 태블릿 |
| `SUR-001` | 시작 안내·동의 | `/s/{kioskToken}` | 고객 | 태블릿 |
| `SUR-002` | 기본 정보 | `/s/{kioskToken}/basic` | 고객 | 태블릿 |
| `SUR-003` | 방문·상담 정보 | `/s/{kioskToken}/preference` | 고객 | 태블릿 |
| `SUR-004` | 모발·두피 정보 | `/s/{kioskToken}/condition` | 고객 | 태블릿 |
| `SUR-005` | 입력 내용 검토 | `/s/{kioskToken}/review` | 고객 | 태블릿 |
| `SUR-006` | 제출 완료 | `/s/{kioskToken}/complete` | 고객 | 태블릿 |
| `ADM-001` | 대시보드 | `/admin/dashboard` | 전체 관리자 | PC |
| `RSP-001` | 응답 목록 | `/admin/responses` | 전체 관리자 | PC |
| `RSP-002` | 응답 상세 | `/admin/responses/{id}` | 범위 내 사용자 | PC |
| `CUS-001` | 고객 목록 | `/admin/customers` | 전체 관리자 | PC |
| `CUS-002` | 고객 상세 | `/admin/customers/{id}` | 범위 내 사용자 | PC |
| `FRM-001` | 설문 템플릿·버전 | `/admin/forms` | 슈퍼관리자 | PC |
| `FRM-002` | 설문 편집기 | `/admin/forms/{id}/edit` | 슈퍼관리자 | PC·태블릿 |
| `FRM-003` | 미리보기·게시 | `/admin/forms/{id}/publish` | 슈퍼관리자 | PC·태블릿 |
| `STR-001` | 매장 목록 | `/admin/stores` | 슈퍼관리자 | PC |
| `STR-002` | 매장 상세 | `/admin/stores/{id}` | 슈퍼관리자 | PC |
| `USR-001` | 사용자 목록 | `/admin/users` | 슈퍼관리자·원장 | PC |
| `USR-002` | 사용자 초대·상세 | `/admin/users/{id}` | 권한 내 관리자 | PC |
| `EXP-001` | 내보내기 이력 | `/admin/exports` | 전체 관리자 | PC |
| `BKP-001` | 백업·복구 | `/admin/backups` | 슈퍼관리자 | PC |
| `AUD-001` | 감사 로그 | `/admin/audit-logs` | 역할별 범위 | PC |
| `ACC-001` | 내 계정 | `/admin/account` | 전체 관리자 | PC·태블릿 |
| `SYS-403` | 접근 권한 없음 | `/403` | 인증 사용자 | 전체 |
| `SYS-404` | 페이지 없음 | `/404` | 전체 | 전체 |
| `SYS-500` | 시스템 오류 | `/500` | 전체 | 전체 |

---

## 7. 화면별 상세 명세

### 7.1 고객 설문 공통 레이아웃

#### 구조

```text
┌──────────────────────────────────────────────┐
│ 로고(좌)               단계 2/4 · 도움말(우) │ 64px
├──────────────────────────────────────────────┤
│ 진행 표시줄 50%                             │ 4px
├──────────────────────────────────────────────┤
│                                              │
│        제목 / 안내                           │
│        질문 카드 및 입력 영역               │ max 760px
│                                              │
├──────────────────────────────────────────────┤
│ 이전                          다음/제출      │ sticky action
└──────────────────────────────────────────────┘
```

- 배경은 `surface-subtle`, 입력 영역은 `surface-default`를 사용한다.
- 본문 최대 너비는 760px, 좌우 여백은 모바일 20px, 태블릿 32px, PC 40px이다.
- 태블릿 세로 화면에서는 본문 상단 여백 32px, 가로 화면에서는 24px을 사용한다.
- 하단 액션 영역은 모바일·태블릿에서 `position: sticky; bottom: 0`으로 고정하고 `env(safe-area-inset-bottom)`을 포함한다.
- 소프트 키보드가 열린 동안 액션 영역이 입력 필드를 가리면 고정을 해제하고 문서 흐름 하단에 배치한다.
- 진행 단계는 `기본 정보`, `방문·상담`, `모발·두피`, `검토`의 4단계이다. 동의와 완료 화면은 진행률 계산에서 제외한다.
- 브라우저 뒤로가기를 눌렀을 때 현재 단계에 저장되지 않은 변경이 있으면 저장 후 이전 단계로 이동한다. 설문 경로 밖으로 나가려 할 때만 이탈 확인 모달을 표시한다.

#### 임시 저장

- 개인정보 동의 완료 시 서버에 `DRAFT` 응답을 생성한다.
- 필드 변경 후 3초간 입력이 없으면 임시 저장한다.
- “다음” 또는 “이전” 선택 시 즉시 저장하고 저장 성공 후 이동한다.
- 상단에는 `저장 중…`, `저장됨`, `저장 실패`를 텍스트와 아이콘으로 표시한다.
- 임시 응답은 마지막 활동 후 30분 동안 유지한다. 25분에 남은 시간을 안내하고 연장 버튼을 제공한다.
- 브라우저 `localStorage`, `IndexedDB`, URL 쿼리에 개인정보와 답변을 저장하지 않는다.
- 네트워크 오류 시 현재 탭 메모리의 입력은 유지하고, 재연결 후 동일 draft ID로 재시도한다.

#### 고객 설문 공통 상태

| 상태 | 화면 처리 |
|---|---|
| 초기 로딩 | 로고·헤더 높이를 유지한 스켈레톤 3개, 10초 초과 시 재시도 안내 |
| 임시 저장 중 | 상단 `저장 중…`, 단계 버튼은 사용 가능하되 이동 전에 저장 완료 대기 |
| 저장 실패 | 상단 고정 경고 배너, `다시 시도` 제공, 제출 버튼 비활성 |
| 오프라인 | “인터넷 연결을 확인해 주세요.” 배너, 입력 유지, 자동 재연결 감지 |
| 세션 만료 | 입력 영역 제거, “설문 시간이 만료되었습니다. 직원을 불러 주세요.” |
| 새 버전 게시 | 현재 설문은 시작 버전으로 계속 진행, 고객에게 알리지 않음 |
| 키오스크 종료 | 모든 입력 제거, 직원 호출 안내 |

### 7.2 `AUTH-001` 관리자 로그인

#### 목적

관리자 계정을 인증하고 역할에 맞는 첫 화면으로 이동한다.

#### 구조 및 규격

- 전체 화면 중앙에 너비 400px 로그인 카드 배치, 모바일은 좌우 20px을 제외한 전체 너비.
- 카드 구성 순서: 로고 40px 높이 → 제목 → 이메일 → 비밀번호 → 로그인 버튼 → 비밀번호 재설정 링크.
- 이메일은 `autocomplete="username"`, 비밀번호는 `autocomplete="current-password"`를 사용한다.
- 비밀번호 보기 버튼은 입력 오른쪽 44×44px 영역에 배치하고 `aria-pressed`를 제공한다.
- 로그인 버튼은 카드 전체 너비, 높이 48px.
- 실패 메시지로 이메일 존재 여부를 구분하지 않는다.

#### 동작

- 성공: 마지막 접근 경로가 권한 내이면 복귀하고, 아니면 `/admin/dashboard`로 이동.
- 5회 연속 실패: 계정과 IP 기준 10분 지연. 화면에는 남은 시간을 표시한다.
- `SUSPENDED`: “사용이 중지된 계정입니다. 관리자에게 문의해 주세요.”
- 세션: 30분 비활동 시 만료. 2분 전에 연장 모달을 표시한다.
- 로그인 화면에는 키오스크 토큰 또는 이전 고객 응답 내용을 표시하지 않는다.

### 7.3 `KSK-001` 키오스크 세션 설정

#### 목적

직원이 고객에게 태블릿을 전달하기 전에 매장과 담당 디자이너를 고정한다.

#### 구조

1. 페이지 제목 “고객 설문 시작”
2. 현재 매장 카드
3. 담당 디자이너 선택
4. 현재 게시 설문 버전 요약
5. “키오스크 시작” 기본 버튼
6. 활성 키오스크 세션 목록

#### 권한·필드

- 슈퍼관리자: 매장을 먼저 검색·선택하고 해당 매장 활성 디자이너를 선택한다.
- 원장: 매장은 읽기 전용, 본인 포함 소속 활성 디자이너를 선택한다.
- 디자이너: 매장과 담당 디자이너가 모두 본인으로 고정된다.
- 디자이너 선택 카드는 프로필 이미지가 없어도 이니셜, 이름, 직급을 표시한다.
- 비활성 디자이너와 비활성 매장은 목록에 표시하지 않는다.

#### 시작 동작

- “키오스크 시작” 선택 시 서버가 128비트 이상 랜덤 토큰과 6자리 종료 PIN을 생성한다.
- 고객용 경로는 관리자 화면과 분리된 새 탭 또는 설치형 PWA 창에서 연다.
- 키오스크 토큰에는 직접적인 사용자·매장 ID를 넣지 않는다.
- 토큰 유효시간은 최대 12시간이며 직원이 언제든 종료할 수 있다.
- 고객용 화면에서는 관리자 쿠키와 관리자 메뉴를 사용할 수 없어야 한다.
- 키오스크 종료는 종료 PIN 또는 관리자 화면의 “세션 종료”로만 가능하다.
- 종료 PIN은 생성 시 한 번만 전체 표시하고 이후 마스킹한다.

### 7.4 `SUR-001` 시작 안내·개인정보 동의

#### 화면 구조

1. 아이디헤어 로고
2. 제목 “더 나은 상담을 위해 고객님의 이야기를 들려주세요”
3. 예상 소요시간 `약 3~5분`
4. 담당 디자이너 표시 카드
5. 만 14세 이상 여부 선택
6. 개인정보 수집·이용 동의 카드
7. 만 14세 미만일 때 법정대리인 입력·동의 카드
8. “동의하고 시작하기” 버튼
9. “동의하지 않음” 텍스트 버튼

#### 동의 카드

- 접힌 상태에서도 `수집 항목`, `수집 목적`, `보유·이용 기간`, `동의 거부 권리와 불이익` 4개 제목과 요약을 표시한다.
- “전체 내용 보기”를 누르면 본문을 확장한다. 스크롤 끝 도달을 동의 조건으로 강제하지 않는다.
- 필수 동의 체크박스 라벨 전체가 클릭 영역이어야 한다.
- 체크박스와 별도로 “동의” 버튼을 중복 배치하지 않는다.
- 동의 문구는 게시된 `consent_text_version` 스냅샷을 사용한다.
- 저장값: 동의 문구 버전, 동의 여부, 고객/법정대리인 구분, 동의 시각, 키오스크 세션 ID, IP, 사용자 에이전트.

#### 미동의·미성년 처리

- “동의하지 않음” 선택 시 확인 모달을 표시한다.
  - 제목: “설문을 종료할까요?”
  - 설명: “동의하지 않으면 고객 정보를 저장하지 않고 설문을 종료합니다.”
  - 버튼: `계속 작성`, `종료`
- 종료 시 서버에 동의 거부 통계용 익명 이벤트만 남길 수 있으며 이름·연락처 등 식별정보는 저장하지 않는다.
- 만 14세 미만이면 법정대리인 성명·연락처·관계와 별도 동의를 모두 받아야 다음 단계로 이동한다.
- 법정대리인 동의 절차를 운영하지 않기로 정책 결정할 경우, `NO` 선택 시 입력을 막고 직원을 호출하도록 설정할 수 있다.

### 7.5 `SUR-002` 기본 정보

#### 구조

- 제목: “기본 정보를 알려주세요”
- 설명: “상담과 고객 확인에 필요한 정보입니다.”
- 입력 순서: 성함 → 성별 → 생년월일 → 연락처 → 주소.
- 768px 이상에서 성함/성별, 생년월일/연락처는 2열이 가능하다. 주소는 항상 1열이다.
- 모바일에서는 모두 1열이다.

#### 입력 동작

- 성함: `inputmode="text"`, 최대 50자.
- 성별: 3개의 선택 카드. 한 행 공간이 부족하면 1열로 줄바꿈.
- 생년월일: 직접 입력과 날짜 선택을 모두 지원. 플레이스홀더 `YYYY-MM-DD`.
- 연락처: `inputmode="tel"`, 입력 중 하이픈 자동 표시, 붙여넣기 허용.
- 주소: 최대 200자. 남은 글자 수는 160자 이상 입력했을 때만 표시.
- 생년월일 입력 결과가 만 14세 미만인데 이전 화면에서 만 14세 이상을 선택했다면 다음 이동을 막고 법정대리인 동의 화면으로 되돌린다.

### 7.6 `SUR-003` 방문·상담 정보

#### 구조

1. 방문동기
2. 소개자 성함(조건부)
3. 원하는 스타일 사진
4. 시술담당 희망 직급
5. 관심 있는 메뉴
6. 원하는 이미지
7. 가장 신경 써야 할 포인트

- `시술담당 희망 직급` 제목 아래에 도움말 `직급별 차등 금액`을 표시한다.

#### 선택 UI

- 태블릿 768px 이상: 선택 카드를 기본 2열, 짧은 선택지는 최대 3열로 배치.
- 모바일: 1열 또는 두 글자 이하 짧은 선택지만 2열.
- 각 카드 최소 높이 52px, 내부 좌우 여백 16px, 체크/라디오 아이콘 24px.
- 다중 선택은 체크 아이콘, 단일 선택은 라디오 아이콘을 사용한다.
- 선택 시 배경·테두리·아이콘을 모두 변경한다. 색상만으로 상태를 전달하지 않는다.
- 배타 옵션을 선택하면 기존 일반 선택을 해제한다. 일반 옵션을 새로 선택하면 배타 옵션을 해제한다.
- `지인소개`를 해제하면 소개자 성함 입력값을 즉시 제거한다.

### 7.7 `SUR-004` 모발·두피 정보

#### 구조

1. 두피 고민
2. 모발 고민
3. 홈케어 구매 이력
4. 선택 문항 안내 “해당하는 항목이 없다면 건너뛰어도 됩니다.”

#### 동작

- 세 필드는 초기 설문에서 선택 사항이다.
- `잘 모르겠음`, `관심없음(잘 모름)`은 해당 필드의 배타 옵션이다.
- 아무것도 선택하지 않고 “다음”을 누르면 경고 모달을 띄우지 않고 검토 단계로 이동한다.
- 선택 수 제한은 없지만 한 필드에 8개를 초과하는 활성 선택지가 생기면 편집기 게시 검토에 “인지 부담 증가” 권고를 표시한다.

### 7.8 `SUR-005` 입력 내용 검토

#### 구조

- 제목: “입력 내용을 확인해 주세요”
- 섹션 카드 3개: 기본 정보 / 방문·상담 정보 / 모발·두피 정보.
- 각 카드 헤더 우측에 `수정` 텍스트 버튼.
- 미입력 선택 문항은 `응답하지 않음`으로 표시.
- 개인정보 동의는 “동의 완료 · YYYY.MM.DD HH:mm”만 표시하고 전문은 링크로 확인한다.
- 하단에 “제출 후에는 고객 화면에서 수정할 수 없습니다.” 안내.
- 버튼: 보조 `이전`, 기본 `설문 제출`.

#### 제출

- 제출 전 전체 필드를 서버에서 다시 검증한다.
- 제출 버튼은 첫 클릭 즉시 비활성화하고 스피너와 `제출 중…`을 표시한다.
- 요청마다 `Idempotency-Key`를 전송한다.
- 성공 응답 전까지 페이지 이탈 경고를 유지한다.
- `409 DUPLICATE_SUBMISSION`이면 기존 성공 응답으로 간주하고 완료 화면으로 이동한다.
- `422 VALIDATION_ERROR`이면 오류가 있는 단계와 필드를 표시하고 해당 단계로 이동한다.
- 10초 초과 시 자동으로 중복 제출하지 않고 “제출 상태 확인” API를 조회한다.

### 7.9 `SUR-006` 제출 완료

#### 구조 및 동작

- 성공 아이콘은 SVG, 64px.
- 제목: “설문이 완료되었습니다”
- 설명: “태블릿을 담당 디자이너에게 전달해 주세요.”
- 개인정보, 선택 답변, 응답 ID 전체값을 표시하지 않는다.
- `다음 고객 설문` 버튼을 제공한다.
- 15초 카운트다운 후 자동 초기화한다. 사용자가 화면을 터치하면 카운트다운을 다시 15초로 연장하지 않는다.
- 초기화 시 브라우저 히스토리를 교체하여 뒤로가기로 이전 고객 응답을 볼 수 없게 한다.
- 서버 임시 draft 토큰과 프론트엔드 메모리 상태를 모두 제거한다.
- 완료 분석 이벤트에는 응답 ID 대신 비식별 이벤트 ID만 사용한다.

### 7.10 `ADM-001` 대시보드

#### 상단 영역

- 페이지 제목, 마지막 갱신 시각, `새로고침`, `엑셀 다운로드`.
- 필터 바: 기간 → 매장 → 디자이너 → 설문 버전 → 추가 항목 필터.
- 기본 기간은 오늘을 포함한 최근 30일.
- 원장과 디자이너의 매장 필터는 현재 매장으로 고정하고 숨기지 말고 읽기 전용 칩으로 표시한다.
- 디자이너에게 디자이너 필터는 표시하지 않는다.
- 필터는 URL 쿼리에 저장하되 이름·전화번호 등 개인정보 검색어는 URL에 넣지 않는다.

#### KPI 카드

| KPI | 정의 | 표시 |
|---|---|---|
| 제출 건수 | 기간 내 `SUBMITTED` 응답 수, `VOIDED` 제외 | 정수, 이전 동기간 대비 증감 |
| 고객 수 | 기간 내 `customer_id` 고유 개수 | 정수 |
| 오늘 제출 | KST 오늘 00:00~현재 제출 수 | 정수 |
| 일평균 제출 | 제출 건수 ÷ 기간 일수 | 소수점 1자리 |
| 활성 매장 | 기간 내 1건 이상 제출한 매장 수 | 슈퍼관리자만 |
| 참여 디자이너 | 기간 내 1건 이상 담당한 디자이너 수 | 슈퍼관리자·원장 |

- 이전 동기간 데이터가 없으면 증감률 대신 `비교 데이터 없음`을 표시한다.
- 증가가 항상 좋은 의미는 아니므로 초록/빨강으로 가치 판단하지 않고 중립 화살표와 텍스트로 표시한다.

#### 차트

1. `기간별 설문 제출 추이`: 선 그래프. 31일 이하는 일, 32~180일은 주, 181일 이상은 월 단위.
2. `방문동기`: 가로 막대. 복수 선택이므로 합계가 고객 수를 초과할 수 있다는 설명 표시.
3. `관심 메뉴`: 가로 막대, 상위 7개와 전체 보기.
4. `원하는 이미지`: 가로 막대.
5. `모발·두피 고민`: 탭 2개로 구분한 가로 막대.
6. `매장별 제출`: 슈퍼관리자만, 정렬 가능한 막대 또는 표.
7. `디자이너별 제출`: 슈퍼관리자·원장만, 상위 10명.

- 모든 차트에는 제목, 기준 기간, 단위, 범례, “데이터 표 보기”를 제공한다.
- 차트 선택 시 해당 값이 필터에 추가되고 응답 목록 링크를 제공한다.
- 데이터가 0건이면 축만 그리지 않고 빈 상태를 표시한다.

#### 레이아웃

- 1440px 기준 KPI 4열, 차트 2열.
- 1024~1199px KPI 2열, 차트 1열.
- 768~1023px KPI 2열, 차트 1열.
- 767px 이하 KPI 1열, 필터는 전체 화면 드로어.

### 7.11 `RSP-001` 응답 목록

#### 검색·필터

- 검색 대상: 고객 성함, 정규화 연락처, 응답 ID.
- 검색은 2자 이상 입력 후 300ms 디바운스. 전화번호는 하이픈 유무를 무시한다.
- 필터: 제출 기간, 매장, 디자이너, 응답 상태, 설문 버전, 방문동기, 관심 메뉴, 원하는 이미지, 모발/두피 고민.
- `필터 초기화`는 기본 기간(최근 30일)을 복원한다.
- 활성 필터는 필터 바 아래 제거 가능한 칩으로 표시한다.

#### 테이블

| 열 | 규칙 |
|---|---|
| 선택 | 체크박스, 현재 페이지 전체 선택 지원 |
| 제출일시 | `YYYY.MM.DD HH:mm`, 기본 내림차순 |
| 고객명 | 목록에서는 마스킹 |
| 연락처 | 목록에서는 마스킹 |
| 매장 | 디자이너 화면에서는 생략 가능 |
| 담당 디자이너 | 디자이너 화면에서는 생략 가능 |
| 관심 메뉴 | 최대 2개 칩 + `외 n` |
| 설문 버전 | `v3` 형식 |
| 상태 | 상태 배지 |
| 작업 | `상세 보기` 아이콘 버튼 |

- 한 페이지 기본 20건, 선택값 20/50/100.
- 행 전체 클릭으로 상세 이동하되 내부 체크박스와 작업 버튼 이벤트는 분리한다.
- 헤더는 세로 스크롤 시 상단 고정.
- 모바일에서는 테이블을 억지로 축소하지 않고 응답 카드 목록으로 전환한다.
- 100건 페이지에서도 첫 의미 있는 렌더 2초 이내를 목표로 한다.

#### 일괄 작업

- 슈퍼관리자·원장: 선택 응답 XLSX 내보내기, 무효 처리.
- 디자이너: 선택 응답 마스킹 XLSX 내보내기만 가능.
- 무효 처리는 사유 10~200자를 필수로 입력하고 되돌리기 가능하도록 한다.

### 7.12 `RSP-002` 응답 상세

#### 구조

1. 뒤로가기, 응답 상태, 제출일시, 작업 메뉴
2. 고객 요약 카드
3. 매장·담당 디자이너·설문 버전 메타데이터
4. 질문 당시 섹션·순서로 답변 표시
5. 동의 이력
6. 변경·감사 타임라인

#### 규칙

- 질문 라벨과 선택지 라벨은 현재 템플릿이 아니라 제출 버전 스냅샷을 사용한다.
- 기본 개인정보는 마스킹하고 `전체 정보 보기` 선택 시 권한 확인 후 공개한다.
- 설문 답변은 제출 후 직접 수정할 수 없다.
- 고객 성함·연락처·생년월일·주소 정정은 `CUS-002`에서 수행하고 응답에는 제출 당시 스냅샷과 현재 고객 정보를 구분해 표시한다.
- 무효 처리 시 기본 통계에서 제외하지만 원본은 유지한다.
- 무효 처리 해제는 슈퍼관리자·소속 원장만 가능하며 사유를 남긴다.
- 인쇄가 필요하면 브라우저 인쇄 스타일을 제공하되 개인정보는 기본 마스킹한다.

### 7.13 `CUS-001` 고객 목록

#### 목적

내부 설문 시스템으로 생성된 고객 DB를 고객 단위로 조회한다.

#### 테이블 열

- 고객명(마스킹)
- 연락처(마스킹)
- 매장
- 최초 설문일
- 최근 설문일
- 최근 담당 디자이너
- 설문 건수
- 상태

#### 규칙

- 같은 매장 내 정규화 연락처가 일치하면 동일 고객 후보로 연결한다.
- 자동 병합으로 기존 고객 정보를 덮어쓰지 않는다.
- 동일 연락처가 발견되면 새 응답은 기존 고객에 연결하되, 이름·생년월일 불일치는 `DATA_REVIEW_REQUIRED` 표시를 남긴다.
- 서로 다른 매장의 동일 연락처는 v1.0에서 자동 병합하지 않는다.
- 디자이너는 본인 담당 응답이 한 건 이상 있는 고객만 볼 수 있고, 다른 디자이너 응답의 존재·건수를 노출하지 않는다.

### 7.14 `CUS-002` 고객 상세

#### 구조

1. 고객 상태·기본정보 카드
2. 개인정보 전체 보기
3. 최초·최근 설문 정보
4. 접근 가능한 설문 이력
5. 정보 정정·파기 요청 이력
6. 감사 로그 요약

#### 정정

- 슈퍼관리자와 소속 원장만 가능.
- 변경 전/후 값, 정정 사유 10~200자, 작업자, 시각을 저장한다.
- 연락처 변경 시 같은 매장 중복 후보를 다시 검사한다.
- 디자이너·매장·제출일은 고객 정정 화면에서 바꿀 수 없다.
- 고객 상태 `DESTRUCTION_PENDING`이면 정정 버튼을 비활성화한다.

#### 파기 요청

- 원장은 파기 요청을 생성할 수 있으나 최종 실행은 슈퍼관리자만 가능하다.
- 실행 전 대상 고객, 연결 응답 수, 백업 잔존 기간, 복구 불가 항목을 표시한다.
- 실제 파기 시 식별정보와 자유 입력값을 정책에 따라 제거하고 통계용 비식별 코드값만 유지할 수 있다.
- 파기 완료 후 기존 URL은 개인을 식별할 수 없는 상태 요약만 표시한다.

### 7.15 `FRM-001` 설문 템플릿·버전

#### 구조

- 현재 게시 버전 카드: 버전, 게시일시, 게시자, 응답 수, 상태.
- 초안 카드: 마지막 수정일, 편집자, 검증 오류 수.
- 버전 이력 테이블: 버전, 상태, 게시 기간, 응답 수, 작업.
- 주 버튼: 초안이 없으면 `새 초안 만들기`, 있으면 `초안 계속 편집`.

#### 작업

- 게시 버전: 미리보기, 새 초안으로 복제.
- 초안: 편집, 미리보기, 삭제.
- 보관 버전: 미리보기, 복제.
- 응답이 연결된 버전은 삭제할 수 없다.
- 초안 삭제는 확인 문구 `초안 삭제` 입력 후 수행하고 감사 로그에 남긴다.

### 7.16 `FRM-002` 설문 편집기

#### 데스크톱 레이아웃

```text
┌──────── 240px ───────┬────── flexible canvas ──────┬──── 320px ────┐
│ 필드 팔레트          │ 단계·섹션·질문 미리보기      │ 속성 패널       │
│ 텍스트/선택/동의     │ 드래그·키보드 순서 변경      │ 라벨/규칙/조건  │
└──────────────────────┴──────────────────────────────┴────────────────┘
```

- 상단 64px 도구막대: 뒤로가기, 버전명, 저장 상태, 실행 취소/다시 실행, 미리보기, 게시 검토.
- 중앙 캔버스 최대 760px로 고객 화면과 동일한 너비를 사용한다.
- 768~1199px에서는 필드 팔레트와 속성 패널을 좌우 드로어로 전환한다.
- 767px 이하에서는 편집을 지원하지 않고 읽기 전용 미리보기와 “태블릿 또는 PC에서 편집해 주세요.”를 표시한다.

#### 필드 추가·정렬

- 필드 팔레트 클릭 또는 드래그로 선택 섹션 하단에 추가한다.
- 드래그 핸들 터치 영역은 44×44px 이상.
- 키보드 사용자를 위해 각 필드 작업 메뉴에 `위로 이동`, `아래로 이동`, `이전 섹션`, `다음 섹션`을 제공한다.
- 이동 후 스크린리더 라이브 영역에 “관심 있는 메뉴를 3번째 위치로 이동했습니다.”처럼 알린다.
- 필드 복제 시 새 `field_key`를 자동 생성하고 라벨 뒤에 “복사본”을 붙인다.

#### 속성 패널

- 공통: 라벨, 도움말, 필수 여부, 내부 `field_key`(생성 후 수정 불가), 활성 여부.
- 텍스트: 최소/최대 길이, 플레이스홀더.
- 선택: 선택지 라벨, `option_code`(생성 후 수정 불가), 순서, 배타 옵션, 최소/최대 선택.
- 조건부 표시: 기준 필드, 연산자, 기준 선택지.
- 동의: 동의명, 전문, 필수 여부, 시행일.
- 필드 삭제 시 참조 조건과 과거 버전 영향 요약을 보여준다. 초안에서만 제거되고 과거 버전 데이터는 유지된다.

#### 저장·충돌

- 변경 2초 후 자동 저장하고, 중요한 구조 변경 전 실행 취소 스택에 기록한다.
- 실행 취소/다시 실행은 현재 브라우저 세션의 최근 50개 작업을 지원한다.
- 서버 `version_revision`이 달라지면 저장을 중단하고 `409 EDIT_CONFLICT` 모달을 표시한다.
- 충돌 시 `최신 버전 다시 불러오기`와 `내 변경 JSON 다운로드`를 제공하며 자동 병합하지 않는다.

### 7.17 `FRM-003` 미리보기·게시

#### 미리보기

- 뷰포트 토글: 모바일 390×844, 태블릿 820×1180, PC 1280×800.
- 실제 설문 컴포넌트를 읽기 전용 테스트 모드로 렌더링한다.
- 조건부 필드를 확인할 수 있도록 테스트 입력을 허용하되 서버 응답으로 저장하지 않는다.
- 필수 오류, 긴 라벨, 최대 선택지, 키보드 포커스를 확인할 수 있어야 한다.

#### 게시 검토

- 오류: 게시 불가. 예: 선택지 1개, 필수 동의 없음, 잘못된 조건.
- 경고: 게시 가능. 예: 한 문항 선택지 8개 초과, 도움말 누락.
- 이전 게시 버전과 `추가`, `삭제`, `라벨 변경`, `필수 변경`, `순서 변경`을 구분해 표시한다.
- 즉시 게시만 v1.0에서 지원한다. 예약 게시는 제외한다.
- 최종 버튼 문구는 `v{n} 게시`.
- 게시 성공 후 새 설문 시작부터 적용된다는 안내와 버전 링크를 제공한다.

### 7.18 `STR-001`, `STR-002` 매장 관리

#### 매장 목록

- 열: 매장명, 매장 코드, 상태, 원장, 활성 디자이너 수, 최근 설문일, 누적 응답 수.
- 검색: 매장명·코드. 필터: 활성/비활성.
- 매장 코드는 영문 대문자·숫자·하이픈 2~20자, 조직 내 유일.

#### 매장 상세

- 기본정보: 매장명 2~50자, 코드, 주소, 대표 연락처, 상태.
- 사용자: 원장, 디자이너 목록과 초대.
- 데이터 요약: 고객 수, 응답 수, 최근 30일 제출.
- 매장 비활성화 전 활성 키오스크 세션·활성 사용자·진행 중 draft 수를 표시한다.
- 비활성화하면 신규 설문과 로그인 범위 배정은 막지만 과거 데이터는 삭제하지 않는다.

### 7.19 `USR-001`, `USR-002` 사용자 관리

#### 사용자 상태

- `INVITED`: 초대 발송, 비밀번호 미설정
- `ACTIVE`: 로그인 가능
- `SUSPENDED`: 로그인·신규 키오스크 불가
- `INVITATION_EXPIRED`: 초대 72시간 만료

#### 초대

- 필드: 이름, 이메일, 역할, 매장, 직급 표시명.
- 슈퍼관리자는 `STORE_OWNER`, `DESIGNER`를 초대할 수 있다.
- 원장은 소속 매장의 `DESIGNER`만 초대할 수 있다.
- 같은 이메일을 중복 초대하지 않는다.
- 초대 재발송은 1분 간격 제한, 일 5회 제한.
- 실제 이메일 발송 시스템이 미구현이면 운영자가 복사할 수 있는 1회용 초대 링크를 제공하고 링크 노출을 감사 로그에 기록한다.

#### 수정

- 역할 변경, 매장 이동, 정지는 확인 모달과 사유를 요구한다.
- 사용자는 자기 역할·매장·상태를 직접 변경할 수 없다.
- 마지막 활성 슈퍼관리자 계정은 정지하거나 역할을 낮출 수 없다.
- 디자이너 정지 후에도 과거 응답의 담당자 이름은 스냅샷으로 유지한다.

### 7.20 `EXP-001` 내보내기 이력

- 목록 열: 생성일시, 요청자, 데이터 범위, 필터 요약, 개인정보 수준, 행 수, 상태, 만료일, 다운로드.
- 상태: `QUEUED`, `RUNNING`, `SUCCEEDED`, `FAILED`, `EXPIRED`, `CANCELED`.
- 진행 중 작업은 5초 간격 폴링 또는 서버 이벤트로 갱신한다.
- 완료 파일은 기본 24시간 후 만료하며 `[POLICY-TBD-03]` 설정으로 변경 가능하다.
- 사용자는 본인이 생성한 파일만 보고, 슈퍼관리자는 전체 이력을 볼 수 있다.
- 원장은 소속 매장 사용자의 이력만 볼 수 있다.
- 다운로드 시점에도 현재 권한을 다시 검사한다. 생성 후 권한이 축소되었다면 다운로드를 거부한다.

### 7.21 `BKP-001` 백업·복구

#### 백업 탭

- 상태 요약: 최근 성공 백업, 다음 자동 백업, 복구 가능 여부.
- 목록: 생성일, 유형 `AUTO`/`MANUAL`/`PRE_RESTORE`, 크기, 체크섬 상태, 생성자, 만료일, 상태.
- 버튼: `지금 백업`.
- 동시에 하나의 백업 또는 복구 작업만 허용한다.

#### 복구 탭

- 복구 가능한 `SUCCEEDED` 백업만 선택.
- 영향 요약: 백업 시각 이후 생성·수정된 매장/사용자/고객/응답/설문 버전 수.
- 1단계 `복구 전 검사`: 스키마 버전, 체크섬, 암호화 키 접근, 파일 완전성 확인.
- 2단계 `복구 확인`: 현재 비밀번호와 확인 문구 `YYYY-MM-DD 백업으로 복구` 입력.
- 모달 외부 클릭과 Esc로 닫지 않는다.
- 복구 중 전 사용자에게 유지보수 화면과 작업 ID를 표시한다.
- 완료 후 슈퍼관리자는 요약 보고서를 내려받을 수 있다.

### 7.22 `AUD-001` 감사 로그

#### 기록 대상

- 로그인 성공·실패·로그아웃
- 개인정보 전체값 열람
- 고객정보 정정·파기 요청·파기
- XLSX 생성·다운로드
- 설문 초안 생성·변경·게시
- 사용자·매장 생성·변경·정지
- 응답 무효 처리·해제
- 백업 생성·복구·실패
- 권한 거부

#### 화면

- 필터: 기간, 작업 유형, 작업자, 매장, 대상 유형, 성공/실패.
- 개인정보 값 자체를 로그에 저장하거나 표시하지 않는다. 변경값은 필요 시 암호화·마스킹된 diff만 보관한다.
- 로그는 UI에서 수정·삭제할 수 없다.
- 원장은 소속 매장 관련 로그만, 디자이너는 본인 작업만 볼 수 있다.

### 7.23 `ACC-001` 내 계정

- 이름, 이메일, 역할, 매장, 최근 로그인은 읽기 전용.
- 비밀번호 변경: 현재 비밀번호, 새 비밀번호, 새 비밀번호 확인.
- 새 비밀번호 최소 10자, 영문·숫자 조합. 붙여넣기와 비밀번호 관리자 사용을 허용한다.
- 비밀번호 변경 성공 시 현재 세션을 제외한 모든 세션을 종료한다.
- “모든 기기에서 로그아웃” 기능 제공.

### 7.24 시스템 화면

| 화면 | 제목 | 설명 | 기본 동작 |
|---|---|---|---|
| `SYS-403` | 접근할 수 없는 페이지입니다 | 현재 계정에는 이 기능 권한이 없습니다. | 대시보드로 이동 |
| `SYS-404` | 페이지를 찾을 수 없습니다 | 주소가 변경되었거나 존재하지 않습니다. | 이전 페이지, 대시보드 |
| `SYS-500` | 잠시 후 다시 시도해 주세요 | 요청을 처리하지 못했습니다. | 재시도, 오류 ID 복사 |
| 유지보수 | 시스템 점검 중입니다 | 복구 작업 상태와 예상 갱신 안내 | 10초마다 상태 확인 |

- 오류 화면에 스택 트레이스, 서버 경로, SQL, 토큰, 개인정보를 노출하지 않는다.

---

## 8. 디자인 시스템

### 8.1 디자인 원칙

1. **고객 설문은 친절하고 단순하게**: 한 화면에 한 주제만 보여주고 선택 결과를 즉시 확인시킨다.
2. **관리자 화면은 데이터 밀도를 유지하되 숨기지 않게**: 주요 필터와 합계는 첫 화면에서 보이고 고급 조건만 접는다.
3. **브랜드는 절제된 흑백 기반으로 표현**: 장식보다 콘텐츠와 상담 데이터를 우선한다.
4. **상태는 색상만으로 구분하지 않기**: 아이콘, 텍스트, 테두리 또는 패턴을 함께 쓴다.
5. **파괴적 작업은 느리게, 반복 작업은 빠르게**: 복구·파기·정지는 확인 단계를 두고 조회·필터·입력은 즉시 반응한다.

### 8.2 색상 팔레트

아이디헤어의 공식 디지털 브랜드 가이드가 별도로 제공되면 브랜드 색상 토큰만 교체한다. 그 전까지 아래 팔레트를 v1.0 기준으로 사용한다.

#### 브랜드·중립 색상

| 토큰 | HEX | 사용처 |
|---|---|---|
| `brand-950` | `#171717` | 주 버튼, 주요 제목, 로고 대체 텍스트 |
| `brand-800` | `#292524` | 주 버튼 hover, 어두운 내비게이션 |
| `brand-700` | `#44403C` | pressed 상태 |
| `accent-700` | `#842E4B` | 강조 hover, 선택 상태 진한 색 |
| `accent-600` | `#A23B5C` | 선택 카드, 링크, 강조 |
| `accent-100` | `#F9DDE7` | 선택 배경, 강조 배지 |
| `accent-50` | `#FDF2F6` | 고객 설문 보조 배경 |
| `neutral-950` | `#0A0A0A` | 주 텍스트 |
| `neutral-700` | `#404040` | 본문 텍스트 |
| `neutral-600` | `#525252` | 보조 텍스트 최소 농도 |
| `neutral-400` | `#A3A3A3` | 비활성 아이콘, placeholder 전용 |
| `neutral-300` | `#D4D4D4` | 진한 구분선 |
| `neutral-200` | `#E5E5E5` | 기본 테두리 |
| `neutral-100` | `#F5F5F5` | 비활성·스켈레톤 배경 |
| `neutral-50` | `#FAFAFA` | 페이지 배경 |
| `white` | `#FFFFFF` | 카드·입력 배경 |

#### 의미 색상

| 토큰 | HEX | 연한 배경 | 사용처 |
|---|---|---|---|
| `success-700` | `#16794A` | `#ECFDF3` | 성공, 활성, 저장 완료 |
| `warning-700` | `#9A6700` | `#FFFAEB` | 주의, 만료 임박 |
| `error-700` | `#B42318` | `#FEF3F2` | 오류, 무효, 파괴적 작업 |
| `info-700` | `#175CD3` | `#EFF8FF` | 정보, 진행 중 |
| `focus` | `#2563EB` | 해당 없음 | 키보드 포커스 링 |

#### 적용 규칙

- 본문 텍스트는 `neutral-700`보다 연한 색을 사용하지 않는다.
- 흰 배경의 16px 일반 텍스트는 최소 4.5:1 명암비를 만족해야 한다.
- 24px 이상 또는 18.66px 굵은 텍스트는 최소 3:1을 만족해야 한다.
- 입력 테두리, 차트 선, 아이콘 등 비텍스트 UI는 주변과 최소 3:1을 목표로 한다.
- `accent-100`, 의미 색상의 연한 배경에는 같은 계열 `700` 텍스트를 사용한다.
- 비활성 상태는 투명도만 낮추지 말고 배경·테두리·커서·`aria-disabled`를 함께 적용한다.

#### 차트 색상

순서대로 반복 사용한다.

1. `#2563EB` 파랑
2. `#EA580C` 주황
3. `#0F766E` 청록
4. `#7E22CE` 보라
5. `#B45309` 황갈
6. `#BE185D` 자홍
7. `#4D7C0F` 연두
8. `#475569` 슬레이트

- 동일 차트에서 8색을 초과하면 “기타”로 묶거나 스크롤 가능한 표로 전환한다.
- 범례 순서와 데이터 표 순서를 동일하게 유지한다.
- 선 그래프는 색상 외에 실선/점선과 포인트 모양을 병행한다.

### 8.3 타이포그래피

#### 폰트 패밀리

```css
font-family:
  "Pretendard Variable",
  Pretendard,
  -apple-system,
  BlinkMacSystemFont,
  "Segoe UI",
  "Noto Sans KR",
  sans-serif;
```

- 숫자는 기본 폰트의 tabular number 기능을 사용한다: `font-variant-numeric: tabular-nums`.
- 웹폰트 실패 시 시스템 폰트로 즉시 대체한다.
- 로고는 제공된 공식 SVG 자산을 사용하고 임의로 유사 폰트로 재현하지 않는다.

#### 타입 스케일

| 토큰 | 크기/행간 | 굵기 | 사용처 |
|---|---|---:|---|
| `display-lg` | 36/46px | 700 | 고객 완료·중요 빈 상태 |
| `heading-xl` | 30/40px | 700 | 고객 설문 페이지 제목 |
| `heading-lg` | 24/34px | 700 | 관리자 페이지 제목 |
| `heading-md` | 20/30px | 600 | 카드·모달 제목 |
| `heading-sm` | 18/28px | 600 | 설문 질문, 섹션 제목 |
| `body-lg` | 18/28px | 400/500 | 고객 설명, 선택 카드 |
| `body-md` | 16/24px | 400/500 | 기본 본문·입력 |
| `body-sm` | 14/20px | 400/500 | 관리자 테이블, 보조 정보 |
| `caption` | 12/18px | 400/500 | 시간·메타데이터. 핵심 정보에는 금지 |

- 고객 설문 입력과 버튼은 16px 미만을 사용하지 않는다.
- 모바일 본문은 16px 이상으로 iOS 자동 확대를 방지한다.
- 한글 장문 본문의 한 줄 길이는 데스크톱 기준 약 40자 이내로 제한한다.
- 양쪽 정렬은 사용하지 않고 왼쪽 정렬한다.

### 8.4 간격·크기 토큰

- 기본 그리드: 4px.
- 주요 간격은 8px 배수로 사용한다.

| 토큰 | 값 | 대표 사용 |
|---|---:|---|
| `space-1` | 4px | 아이콘 내부 |
| `space-2` | 8px | 라벨-도움말, 칩 간격 |
| `space-3` | 12px | 작은 컴포넌트 내부 |
| `space-4` | 16px | 입력 간격, 버튼 좌우 |
| `space-5` | 20px | 모바일 페이지 여백 |
| `space-6` | 24px | 카드 패딩, 섹션 간격 |
| `space-8` | 32px | 태블릿 페이지 여백 |
| `space-10` | 40px | 큰 섹션 |
| `space-12` | 48px | 고객 단계 간 구분 |
| `space-16` | 64px | 페이지 블록 구분 |

### 8.5 모서리·테두리·그림자

| 토큰 | 값 | 사용 |
|---|---|---|
| `radius-sm` | 6px | 배지, 작은 컨트롤 |
| `radius-md` | 10px | 입력, 버튼 |
| `radius-lg` | 14px | 카드, 선택 카드 |
| `radius-xl` | 20px | 고객 설문 메인 카드 |
| `radius-full` | 9999px | 상태 칩, 아바타 |
| `border-default` | 1px solid `neutral-200` | 기본 카드·입력 |
| `shadow-sm` | `0 1px 2px rgba(0,0,0,.06)` | 입력 hover, 작은 팝오버 |
| `shadow-md` | `0 8px 24px rgba(0,0,0,.10)` | 드롭다운, 모달 |
| `shadow-lg` | `0 20px 48px rgba(0,0,0,.16)` | 전체 화면 위 중요 모달 |

- hover에서 컴포넌트 크기나 위치를 바꾸는 scale 애니메이션을 사용하지 않는다.
- 선택 상태는 기본 1px 테두리를 2px로 바꾸되 `box-sizing: border-box`로 레이아웃 이동을 방지한다.

### 8.6 레이아웃 규칙

#### 관리자 셸

- 데스크톱(≥1200px): 왼쪽 사이드바 240px, 상단바 64px, 콘텐츠 최대 1440px.
- 태블릿(768~1199px): 사이드바 72px 아이콘형, 상단바 64px.
- 모바일(≤767px): 사이드바 숨김, 상단바 56px, 메뉴 버튼으로 전체 화면 드로어.
- 콘텐츠 패딩: 모바일 20px, 태블릿 24px, 데스크톱 32px.
- 페이지 제목과 필터 사이 24px, 필터와 데이터 사이 24px.

#### 그리드

- 1200px 이상: 12열, gutter 24px.
- 768~1199px: 8열, gutter 20px.
- 767px 이하: 4열, gutter 16px.
- 같은 페이지의 카드 시작선과 표 시작선을 맞춘다.
- 가로 스크롤은 데이터 표 컨테이너 내부에서만 허용하고 페이지 전체에는 금지한다.

#### z-index

| 계층 | 값 |
|---|---:|
| 기본 콘텐츠 | 0 |
| sticky 헤더·액션 | 10 |
| 드롭다운·툴팁 | 20 |
| 드로어·오버레이 | 30 |
| 모달 | 40 |
| 토스트 | 50 |

---

## 9. 공통 컴포넌트 규격

### 9.1 버튼

#### 종류

| 종류 | 사용 | 배경/텍스트 |
|---|---|---|
| Primary | 단계 진행, 저장, 게시 등 화면당 핵심 1개 | `brand-950` / white |
| Secondary | 이전, 미리보기, 보조 작업 | white / `neutral-950`, 테두리 |
| Tertiary | 낮은 우선순위, 표 내부 작업 | transparent / `neutral-700` |
| Destructive | 파기, 정지, 복구 실행 | `error-700` / white |
| Link | 문서 보기, 상세 이동 | transparent / `accent-700` |

#### 크기

| 크기 | 높이 | 좌우 패딩 | 텍스트 | 아이콘 |
|---|---:|---:|---:|---:|
| `lg` | 52px | 20px | 16px/600 | 20px |
| `md` | 44px | 16px | 14px/600 | 18px |
| `sm` | 36px | 12px | 14px/600 | 16px |
| 아이콘 전용 | 44×44px 기본 | 해당 없음 | 해당 없음 | 20px |

- 고객 설문은 `lg`, 관리자 기본은 `md`를 사용한다.
- 버튼 간 간격 8px. 동일 우선순위 버튼을 3개 이상 나란히 배치하지 않는다.
- 로딩 시 기존 너비를 유지하고 아이콘과 문구를 스피너+진행 문구로 교체한다.
- 로딩·비활성 버튼은 중복 실행을 막지만 설명이 필요한 경우 툴팁 또는 인접 도움말을 제공한다.
- 포커스: 외곽 2px `focus`, 요소와 링 사이 2px white 간격.

### 9.2 입력창

#### 구조

```text
라벨 [필수]
┌──────────────────────────────┐
│ prefix  입력값        suffix │ 52px 고객 / 44px 관리자
└──────────────────────────────┘
도움말 또는 오류 메시지
```

- 라벨과 입력 간격 8px, 입력과 도움말 6px.
- 고객 입력 높이 52px, 관리자 입력 44px.
- 좌우 패딩 14px, 텍스트 16px 고객/14px 관리자.
- placeholder는 라벨을 대체하지 않는다.
- 오류 상태: `error-700` 2px 테두리, 오류 아이콘, 텍스트 메시지, `aria-describedby`.
- 읽기 전용은 흰 배경+점선 테두리보다 `neutral-50` 배경과 잠금 아이콘을 사용한다.
- 비활성은 `neutral-100` 배경, `neutral-400` 텍스트, `not-allowed`.
- 자동완성 가능한 이름·전화·주소는 적절한 `autocomplete` 토큰을 사용한다.

### 9.3 체크박스·라디오·선택 카드

- 시각 컨트롤 24×24px, 전체 라벨 터치 영역 최소 48px 높이.
- 체크박스는 다중, 라디오는 단일 선택에만 사용한다.
- 선택 카드:
  - 최소 높이 52px
  - 패딩 12px 16px
  - 기본 테두리 `neutral-200`
  - hover `neutral-300`
  - 선택 테두리 2px `accent-600`, 배경 `accent-50`
  - focus는 선택 여부와 별도로 파란 포커스 링
- 카드 내부 설명이 2줄 이상이면 컨트롤을 상단 정렬한다.

### 9.4 셀렉트·멀티셀렉트

- 관리자 필터에 사용하고 고객 설문 선택 문항에는 기본적으로 선택 카드를 사용한다.
- 옵션이 8개 이상이면 검색형 셀렉트를 허용한다.
- 드롭다운 최대 높이 320px, 넘으면 내부 스크롤.
- 키보드: 위/아래 이동, Enter 선택, Esc 닫기, 입력 시 검색.
- 선택값 3개까지 칩으로 표시하고 초과분은 `+n`.
- “전체”는 실제 값으로 저장하지 않고 필터 없음 상태로 처리한다.

### 9.5 날짜 선택기

- 직접 입력과 달력 선택을 모두 허용한다.
- 날짜 형식은 입력 중 `YYYY-MM-DD`, 표시 텍스트는 `YYYY.MM.DD`.
- 관리자 기간 필터는 시작일·종료일을 한 컴포넌트로 제공한다.
- 종료일은 시작일보다 빠를 수 없다.
- 자주 쓰는 범위: 오늘, 최근 7일, 최근 30일, 이번 달, 지난달.
- 날짜 해석은 KST 기준이며 종료일 23:59:59.999까지 포함한다.

### 9.6 카드

| 카드 | 패딩 | 용도 |
|---|---:|---|
| 고객 설문 카드 | 모바일 20px, 태블릿 28px | 질문 그룹 |
| 관리자 기본 카드 | 24px | KPI, 차트, 정보 |
| 밀집 카드 | 16px | 목록 모바일 카드 |
| 선택 카드 | 12px 16px | 라디오·체크 선택 |

- 카드 제목과 본문 간격 16px.
- 중첩 카드는 최대 1단계만 허용한다.
- 클릭 가능한 카드에는 hover 테두리, pointer 커서, 명확한 링크 목적을 제공한다.

### 9.7 테이블

- 헤더 높이 44px, 본문 행 최소 52px.
- 셀 좌우 패딩 16px, 표 텍스트 14px/20px.
- 헤더 배경 `neutral-50`, 본문 white, 행 구분선 `neutral-200`.
- 정렬 가능한 헤더는 버튼 요소로 구현하고 정렬 방향 아이콘과 `aria-sort`를 제공한다.
- 첫 열 선택 체크박스는 44px 영역.
- 빈 셀은 `-`로 표시하되 스크린리더에는 `값 없음`을 제공한다.
- 긴 값은 한 줄 말줄임 후 툴팁을 제공한다. 핵심 식별정보는 최소 2열 폭을 확보한다.
- 모바일 카드 전환 시 각 값 앞에 열 라벨을 명시한다.

### 9.8 상태 배지·칩

- 높이 24px, 좌우 패딩 8px, 12px/500.
- 배지는 상태 표시용, 칩은 선택·필터 제거용으로 구분한다.
- 상태 라벨은 한글 전체 단어를 사용한다. 아이콘만 표시하지 않는다.
- 상태 예시:
  - 활성: 초록 점 + `활성`
  - 초안: 회색 문서 아이콘 + `초안`
  - 진행 중: 파랑 스피너 + `진행 중`
  - 실패: 빨강 경고 아이콘 + `실패`

### 9.9 모달

#### 크기

| 유형 | 너비 |
|---|---:|
| 확인 | 420px |
| 기본 폼 | 560px |
| 상세·비교 | 720px |
| 전체 화면 | 모바일에서만 100vw × 100dvh |

- 데스크톱 최대 높이 `min(80dvh, 800px)`, 본문만 스크롤하고 제목·버튼은 고정.
- 배경 오버레이 `rgba(0,0,0,.48)`.
- 열릴 때 첫 의미 있는 요소 또는 제목으로 포커스를 이동한다.
- 닫힐 때 호출한 요소로 포커스를 복귀시킨다.
- 포커스가 모달 밖으로 나가지 않게 한다.
- 일반 모달은 Esc로 닫을 수 있다.
- 복구·파기·게시 등 파괴적 모달은 Esc와 외부 클릭 닫기를 금지하고 명시적 `취소`만 제공한다.
- 버튼 순서: 왼쪽 `취소`, 오른쪽 기본/파괴 작업.

### 9.10 드로어·바텀시트

- 관리자 모바일 필터는 오른쪽 전체 높이 드로어.
- 태블릿 편집기 속성은 오른쪽 360px 드로어.
- 고객 화면에서는 확인이 필요한 단순 선택에 높이 70dvh 이하 바텀시트를 사용할 수 있다.
- 드로어도 모달과 동일한 포커스 잠금·복귀 규칙을 적용한다.
- 스와이프만으로 닫는 기능은 금지하고 닫기 버튼을 제공한다.

### 9.11 토스트·배너

- 토스트 위치: 데스크톱 우측 상단 24px, 모바일 하단 액션 위 16px.
- 성공 4초, 정보 6초, 오류는 사용자가 닫거나 문제가 해소될 때까지 유지.
- 중요한 오류는 토스트만 쓰지 않고 해당 영역 인라인 메시지 또는 페이지 배너를 병행한다.
- 토스트 최대 3개. 추가 메시지는 큐 처리한다.
- `aria-live`: 성공·정보 `polite`, 제출 실패·세션 만료 `assertive`.

### 9.12 빈 상태·로딩

- 초기 페이지 로딩: 실제 레이아웃과 같은 크기의 스켈레톤.
- 버튼 단일 작업: 버튼 내부 스피너.
- 1초 이내 예상 작업에는 전체 화면 스피너를 쓰지 않는다.
- 빈 상태는 다음을 구분한다.
  - 최초 데이터 없음: 설명과 시작 행동 제공.
  - 필터 결과 없음: `필터 초기화` 제공.
  - 권한 범위에 데이터 없음: 권한 설명, 범위 확대 유도 금지.
- 스켈레톤 애니메이션은 `prefers-reduced-motion: reduce`에서 정지한다.

### 9.13 페이지네이션

- `이전`, 페이지 번호, `다음`, 페이지 크기 선택을 제공한다.
- 모바일은 `이전`, `현재/전체`, `다음`으로 단순화한다.
- 페이지 변경 후 목록 제목으로 포커스를 이동하지 않고 표 시작 안내를 라이브 영역에 알린다.
- 필터 변경 시 1페이지로 초기화한다.

### 9.14 검색

- 검색 입력 오른쪽에 값이 있을 때만 지우기 버튼을 표시한다.
- 결과 로딩 중 스피너를 표시하되 키 입력을 막지 않는다.
- 개인정보 검색어를 브라우저 기록, URL, 분석 도구 이벤트에 저장하지 않는다.
- 검색 결과 수를 “총 24건”처럼 입력 아래 또는 표 헤더에 표시한다.

### 9.15 아이콘

- Lucide 또는 동일한 24px stroke 기반 SVG 세트 하나만 사용한다.
- 기본 20px, 작은 표 작업 18px, 빈 상태 48px.
- 이모지를 기능 아이콘으로 사용하지 않는다.
- 아이콘 전용 버튼은 `aria-label`과 툴팁을 제공한다.

---

## 10. 반응형 상세 기준

### 10.1 브레이크포인트

| 이름 | 범위 | 기준 기기 |
|---|---|---|
| `mobile` | 360~767px | 모바일 세로·가로 |
| `tablet` | 768~1199px | 8~13인치 태블릿 세로·가로 |
| `desktop` | 1200~1599px | 노트북·일반 모니터 |
| `wide` | 1600px 이상 | 대형 모니터 |

- 360px 미만에서도 콘텐츠가 잘리지 않아야 하지만 공식 QA 최소 너비는 360px이다.
- 기기명이나 user-agent가 아니라 CSS 컨테이너/뷰포트 너비로 전환한다.
- 확대 200%에서도 주요 흐름을 사용할 수 있어야 한다.

### 10.2 고객 설문

| 항목 | 모바일 | 태블릿 | 데스크톱 |
|---|---|---|---|
| 본문 너비 | 100% | 최대 760px | 최대 760px |
| 페이지 여백 | 20px | 32px | 40px |
| 질문 카드 열 | 1열 | 1열 | 1열 |
| 선택지 열 | 1~2열 | 2~3열 | 2~3열 |
| 하단 버튼 | 가로 2개, 다음 버튼 우선 폭 | 가로 2개 | 카드 하단 가로 |
| 헤더 | 56px | 64px | 64px |
| 질문 간격 | 32px | 40px | 40px |

- 모바일에서 `이전` 40%, `다음` 60%를 기본으로 한다.
- 첫 단계처럼 이전이 없으면 다음 버튼 100%.
- 가로 태블릿에서도 질문을 좌우 두 컬럼으로 나누지 않는다. 한 주제의 읽기 흐름을 유지한다.

### 10.3 관리자

- 767px 이하에서 표는 카드 목록으로 변환하고 차트는 1열.
- 필터 3개 이상은 `필터` 버튼과 활성 개수 배지로 접는다.
- 768~1199px에서 사이드바는 아이콘형이며 hover에 의존하지 않고 클릭 시 툴팁/라벨을 제공한다.
- 설문 편집은 768px 이상에서만 가능하다.
- 대시보드 차트 최소 높이 300px, 모바일 260px.
- 모달 너비가 뷰포트-40px보다 크면 전체 화면으로 전환한다.

### 10.4 검증 뷰포트

QA는 최소 다음 크기에서 가로 스크롤, 잘림, 고정 영역 겹침을 검사한다.

- 360×800
- 390×844
- 768×1024
- 820×1180
- 1024×768
- 1280×800
- 1440×900
- 1920×1080

---

## 11. 접근성 원칙

### 11.1 준수 목표

- WCAG 2.2 AA를 기본 목표로 한다.
- 터치 대상은 WCAG 최소 기준보다 넉넉한 44×44px 이상, 고객 설문 주요 컨트롤은 48×48px 이상으로 한다.
- 키보드, 터치, 마우스 어느 하나에 종속된 기능을 만들지 않는다.

### 11.2 시맨틱 구조

- 페이지마다 유일한 `h1`을 사용하고 단계·카드 제목은 계층 순서대로 `h2`, `h3`를 사용한다.
- 질문 그룹은 `fieldset`과 `legend`로 묶는다.
- 모든 입력은 보이는 `label`과 연결한다.
- 필수 여부는 시각 표시와 `aria-required`를 함께 적용한다.
- 현재 단계는 `aria-current="step"`으로 알린다.
- 장식 이미지는 빈 `alt`, 의미 있는 이미지는 목적을 설명하는 `alt`를 제공한다.

### 11.3 키보드·포커스

- Tab 순서는 시각적 읽기 순서와 일치한다.
- 드래그 정렬 기능에는 버튼 기반 대체 동작을 제공한다.
- 모든 인터랙티브 요소에 2px 이상 명확한 포커스 링을 표시한다.
- sticky 헤더·하단 액션이 포커스 요소를 가리지 않도록 `scroll-margin`을 적용한다.
- 모달·드로어는 포커스를 내부에 가두고 닫은 뒤 호출 요소로 돌려보낸다.
- 포커스를 임의로 페이지 처음으로 이동시키지 않는다. 오류 또는 단계 전환 등 명확한 경우에만 이동한다.

### 11.4 오류·상태 안내

- 오류는 색상, 아이콘, 텍스트를 함께 사용한다.
- 제출 검증 실패 시 상단 오류 요약에 오류 수와 필드 링크를 제공한다.
- 비동기 상태는 `aria-live`로 알리되 잦은 자동 저장은 5초 이내 반복 안내하지 않는다.
- 시간 제한은 만료 5분 전에 안내하고 사용자가 연장할 수 있게 한다.
- 인증에 퍼즐·기억력 테스트를 요구하지 않는다.

### 11.5 차트

- Chart.js `canvas`에 `role="img"`와 데이터 요약 `aria-label`을 제공한다.
- 모든 차트에 동일 데이터의 HTML 표를 제공한다.
- 차트 hover 툴팁 정보는 키보드 포커스와 표에서도 확인할 수 있어야 한다.
- 색상만으로 계열을 구분하지 않고 범례 텍스트, 점 모양, 선 패턴을 병행한다.

### 11.6 동작·애니메이션

- 마이크로 인터랙션은 150~250ms, 모달은 최대 300ms.
- 위치 변화는 `transform`, 노출 변화는 `opacity`를 사용한다.
- 자동 재생, 깜빡임, 과도한 패럴랙스를 사용하지 않는다.
- `prefers-reduced-motion: reduce`에서 필수적이지 않은 애니메이션을 제거한다.

---

## 12. 상태값 정의

### 12.1 도메인 상태

#### 설문 버전 `survey_version.status`

| 값 | 의미 | 허용 작업 |
|---|---|---|
| `DRAFT` | 편집 중 | 수정, 검증, 미리보기, 삭제, 게시 |
| `PUBLISHED` | 신규 설문에 사용 | 미리보기, 복제 |
| `ARCHIVED` | 이전 게시 버전 | 미리보기, 복제 |

#### 응답 `survey_response.status`

| 값 | 의미 | 통계 포함 | 수정 |
|---|---|:---:|---|
| `DRAFT` | 고객 작성 중 | X | 고객 세션만 |
| `SUBMITTED` | 제출 완료 | O | 답변 수정 불가 |
| `VOIDED` | 중복·테스트 등으로 무효 | X | 상태 해제만 |
| `DESTRUCTION_PENDING` | 파기 검토 중 | X | 정정 불가 |
| `DESTROYED` | 식별정보 파기 완료 | 비식별 집계만 | 불가 |

#### 고객 `customer.status`

| 값 | 의미 |
|---|---|
| `ACTIVE` | 정상 조회 가능 |
| `DATA_REVIEW_REQUIRED` | 동일 연락처의 기본정보 불일치 확인 필요 |
| `DESTRUCTION_PENDING` | 파기 승인 대기 |
| `DESTROYED` | 식별정보 제거 |

#### 매장 `store.status`

| 값 | 의미 |
|---|---|
| `ACTIVE` | 신규 설문·사용자 배정 가능 |
| `INACTIVE` | 과거 조회만 가능 |

#### 사용자 `user.status`

| 값 | 의미 |
|---|---|
| `INVITED` | 초대 수락 전 |
| `ACTIVE` | 로그인 가능 |
| `SUSPENDED` | 로그인 불가 |
| `INVITATION_EXPIRED` | 초대 만료 |

#### 비동기 작업

`QUEUED` → `RUNNING` → `SUCCEEDED` 또는 `FAILED`; 사용자가 취소할 수 있는 대기 작업만 `CANCELED`; 파일 보존기한 종료 시 `EXPIRED`.

### 12.2 폼 화면 상태

| 상태 | 조건 | UI |
|---|---|---|
| `PRISTINE` | 변경 없음 | 저장 상태 표시 안 함 |
| `DIRTY` | 저장되지 않은 변경 | `변경됨` |
| `SAVING` | 저장 요청 중 | `저장 중…` |
| `SAVED` | 서버 저장 완료 | `저장됨 HH:mm` |
| `INVALID` | 검증 오류 | 오류 요약·필드 메시지 |
| `SUBMITTING` | 최종 제출 중 | 제출 버튼 로딩·중복 클릭 차단 |
| `SUBMIT_FAILED` | 제출 실패 | 입력 유지·재시도 |
| `SESSION_EXPIRED` | draft 만료 | 입력 제거·직원 호출 |

### 12.3 컴포넌트 상태

모든 버튼, 입력, 선택 카드에는 아래 상태를 디자인 파일과 Storybook에 각각 구현한다.

- `default`
- `hover`
- `pressed`
- `focus-visible`
- `filled/selected`
- `error`
- `readonly`(입력만)
- `loading`(버튼·데이터 컴포넌트)
- `disabled`

---

## 13. 대시보드·필터·집계 상세 규칙

### 13.1 공통 필터 객체

```json
{
  "date_from": "2026-07-05",
  "date_to": "2026-08-03",
  "store_ids": [],
  "designer_user_ids": [],
  "survey_version_ids": [],
  "response_statuses": ["SUBMITTED"],
  "answer_filters": [
    {
      "field_key": "interested_services",
      "operator": "includes_any",
      "option_codes": ["CUT", "PERM"]
    }
  ]
}
```

- 기본 응답 상태는 `SUBMITTED`만 포함한다.
- 다중 항목 필터의 같은 필드 내 선택지는 OR, 서로 다른 필드는 AND로 결합한다.
- 예: 관심 메뉴 `컷 또는 펌`이면서 원하는 이미지 `자연스러운`.
- 필터 옵션에는 현재 권한 범위에서 존재하는 값만 노출한다.
- 차트·KPI·표·XLSX API는 동일한 필터 스키마를 사용한다.

### 13.2 집계 규칙

- 제출일 기준은 `submitted_at`, KST 경계로 계산한다.
- `VOIDED`, `DRAFT`, `DESTRUCTION_PENDING`은 기본 집계에서 제외한다.
- 다중 선택 문항의 옵션별 건수는 각 옵션 선택을 1건으로 센다.
- 다중 선택 비율 분모는 해당 문항에 1개 이상 답한 응답 수이다.
- 선택 문항 미응답은 별도 `응답 없음` 값으로 표에서 제공하되 기본 차트에서는 숨긴다.
- 파기된 고객의 비식별 선택 답변을 유지하는 정책이 승인된 경우에만 집계에 포함한다.
- 값이 5건 미만인 세부 교차 집계는 개인정보 재식별 위험 검토 후 표시 여부를 설정한다 `[POLICY-TBD-04]`.

### 13.3 차트 공통 옵션

```text
responsive: true
maintainAspectRatio: false
animation.duration: 200ms
locale: ko-KR
tooltip intersect: false
legend position: bottom
```

- 축은 0부터 시작하며 건수에 소수 눈금을 표시하지 않는다.
- 막대는 값 내림차순을 기본으로 한다.
- 데이터 레이블은 막대 수가 10개 이하일 때만 기본 표시한다.
- 화면 크기 변경 시 150ms 디바운스로 차트를 다시 그린다.
- 차트 이미지만 다운로드하는 기능은 v1.0 범위에서 제외한다.

---

## 14. XLSX 내보내기 명세

### 14.1 생성 조건

- 내보내기 대상은 요청자의 서버 권한 범위와 현재 필터의 교집합이다.
- 생성 전 예상 응답 수와 개인정보 포함 수준을 표시한다.
- 50,000 응답 이하: 동기 또는 짧은 비동기 생성.
- 50,000 응답 초과: 백그라운드 작업으로 전환하고 완료 알림을 표시한다.
- 동일 사용자는 동시에 최대 3개 작업만 실행할 수 있다.
- 서버 생성 제한시간은 10분. 초과 시 `FAILED`와 재시도 가능한 오류 코드를 기록한다.

### 14.2 파일명

```text
idhair_survey_{scope}_{dateFrom}_{dateTo}_{generatedAtKST}.xlsx
```

예: `idhair_survey_GANGNAM01_20260701_20260731_20260803T143500.xlsx`

- 파일명에는 고객명, 연락처, 사용자 이메일을 포함하지 않는다.
- `scope`는 본사 `ALL`, 매장 코드, 디자이너 내부 비식별 코드 중 하나이다.

### 14.3 시트 구조

#### `설문목록`

응답 1건당 1행.

| 열 | 값 |
|---|---|
| 응답ID | 외부 표시용 짧은 ID |
| 상태 | 제출 완료/무효/파기 등 한글 라벨 |
| 제출일시_KST | `YYYY-MM-DD HH:mm:ss` |
| 매장코드 | 제출 시점 코드 |
| 매장명 | 제출 시점 이름 |
| 담당디자이너ID | 비식별 내부 코드 |
| 담당디자이너명 | 제출 시점 표시명 |
| 설문버전 | `v3` |
| 고객ID | 외부 표시용 짧은 ID |
| 성함 | 내보내기 수준에 따라 전체/마스킹 |
| 성별 | 한글 라벨 |
| 생년월일 | 전체/마스킹/공란 |
| 연락처 | 전체/마스킹 |
| 주소 | 전체 개인정보 포함일 때만, 그 외 공란 |
| 방문동기 | 세로 막대 문자로 구분한 라벨 |
| 관심메뉴 | 세로 막대 문자로 구분한 라벨 |

#### `응답상세`

응답과 질문 조합당 1행. 버전별 필드가 달라도 열 구조가 변하지 않는다.

| 열 | 값 |
|---|---|
| 응답ID | `설문목록`과 연결 |
| 설문버전 | 버전 번호 |
| 섹션순서 | 정수 |
| 섹션명 | 제출 당시 라벨 |
| 질문순서 | 정수 |
| field_key | 안정된 내부 키 |
| 질문 | 제출 당시 라벨 |
| 유형 | `SHORT_TEXT` 등 |
| 답변코드 | 단일 또는 세로 막대 문자로 구분한 option code |
| 답변라벨 | 단일 또는 세로 막대 문자로 구분한 당시 라벨 |
| 미응답여부 | `Y`/`N` |

#### `코드북`

- 설문 버전, `field_key`, 질문 라벨, `option_code`, 선택지 라벨, 필수 여부, 활성 기간.
- 다중 선택의 집계 방식과 파일 생성 필터 설명.

#### `내보내기정보`

- 생성일시, 생성자 역할, 데이터 범위, 필터 JSON의 사람이 읽을 수 있는 요약, 개인정보 수준, 총 응답 수, 시간대, 문서 버전.
- 생성자의 이메일·IP는 파일에 넣지 않는다.

### 14.4 데이터 안전

- `=`, `+`, `-`, `@`로 시작하는 사용자 입력은 앞에 작은따옴표를 붙여 Excel 수식 실행을 방지한다.
- 셀 값은 UTF-8 원본을 XLSX 문자열로 변환하고 HTML을 포함하지 않는다.
- 한 셀 최대 길이를 초과하면 잘라내지 말고 생성 실패로 처리한 뒤 대상 응답 ID를 로그에 기록한다. 로그에는 실제 값은 남기지 않는다.
- 파일은 서버에서 암호화 보관하고 서명된 1회성 다운로드 URL을 사용한다.
- 다운로드 URL 유효시간은 10분, 파일 자체 보존은 기본 24시간.
- 생성, 다운로드, 만료, 실패를 감사 로그에 기록한다.

### 14.5 UI 흐름

1. 현재 필터 요약
2. 예상 응답 건수
3. 개인정보 수준 선택
   - `마스킹된 기본 내보내기` 기본값
   - `개인정보 전체값 포함` 슈퍼관리자·원장만
4. 전체값 포함 시 사용 사유 10~200자
5. 확인 체크 “다운로드한 파일을 승인된 목적과 기간 내에서만 보관합니다.”
6. `파일 생성` 버튼

---

## 15. 백업·복구 상세 명세

### 15.1 백업 범위

백업에는 다음을 포함한다.

- 조직, 매장, 사용자·역할·상태
- 고객과 설문 응답
- 동의 문구 버전과 동의 이력
- 설문 템플릿·버전·필드·선택지
- 감사 로그
- 내보내기 작업 메타데이터
- 애플리케이션 설정과 정책 값

다음은 포함하지 않는다.

- 비밀번호 원문
- 세션 토큰, 키오스크 토큰, 초대 토큰
- 암호화 마스터 키와 외부 서비스 비밀키
- 이미 만료된 XLSX 파일
- 애플리케이션 바이너리와 배포 소스

### 15.2 백업 정책

- 자동 백업 주기·보존기간은 설정 가능해야 한다.
- 제안 초기값: 매일 02:00 KST 전체 백업, 최근 30개 일간 백업 보관.
- 실제 운영값은 개인정보 보유·파기 정책과 함께 `[POLICY-TBD-02]`로 확정한다.
- 수동 백업은 슈퍼관리자만 생성한다.
- 백업 파일은 운영 DB와 다른 저장소·권한 경계에 암호화해 보관한다.
- 백업마다 SHA-256 이상의 체크섬, 스키마 버전, 레코드 수, 생성 완료 시각을 기록한다.
- 월 1회 이상 자동 복구 검증 환경에서 복원 테스트를 수행하고 결과만 운영 화면에 표시한다.

### 15.3 복구 안전 규칙

- 운영 복구는 삭제·덮어쓰기 위험이 있으므로 슈퍼관리자 재인증과 확인 문구가 필수다.
- 복구 대상 백업의 스키마 버전이 현재 애플리케이션과 호환되지 않으면 실행을 막는다.
- 복구 직전 `PRE_RESTORE` 전체 백업에 성공하지 못하면 복구를 시작하지 않는다.
- 복구 트랜잭션 또는 단계별 체크포인트를 사용해 부분 복구 상태로 서비스를 열지 않는다.
- 복구 성공 후 다음 수치를 백업 메타데이터와 비교한다.
  - 매장 수
  - 사용자 수
  - 고객 수
  - 응답 수
  - 설문 버전 수
  - 감사 로그 마지막 시각
- 불일치하면 서비스 읽기 전용을 유지하고 자동 롤백한다.
- 복구 보고서에는 레코드 수와 오류 코드만 포함하고 개인정보를 넣지 않는다.

---

## 16. 개인정보·보안 기준

### 16.1 동의 문구 구조

#### 종이 상담카드 확정 원문

2026-08-03 제공된 전사본을 기준으로 현재 종이 카드에는 다음 문구가 사용된다.

> 저희 매장은 수집한 개인정보를 다음의 목적으로 보유하고 활용합니다.
>
> 1. 개인정보 수집 항목: 성명 / 생년월일 / 주소 / 핸드폰  
> 2. 개인정보의 수집 목적: 개인식별 / 공지사항의 전달 / 매장이벤트 및 할인 안내  
> 3. 개인정보 보유 및 이용기간: 회원 탈퇴 시 삭제
>
> 개인정보 수집 동의: 동의 / 동의하지 않음

이 문구는 종이 카드 원문 추적을 위한 `LEGACY_PAPER_V1` 동의 버전으로 보관한다. 디지털 운영 문구는 다음 사유로 원문을 그대로 게시하지 않고 정책 확정 후 별도 버전으로 게시한다.

- 신규 시스템에는 고객 회원가입·회원 탈퇴 기능이 없으므로 `회원 탈퇴 시 삭제`를 실제로 실행 가능한 보유기간 또는 파기 조건으로 교체해야 한다.
- `매장이벤트 및 할인 안내`를 유지한다면 상담을 위한 필수 동의와 분리한 선택적 마케팅 동의로 구현한다.
- 마케팅 동의에는 발송 채널, 수집 항목, 이용 목적, 보유기간, 거부 시 불이익 없음을 별도로 표시한다.
- 실제 디지털 수집 항목은 `성명 / 휴대전화번호 / 생년월일(선택) / 주소(선택)`처럼 필수·선택 여부를 구분한다.

#### 디지털 동의 문구 필수 구조

필수 개인정보 수집·이용 동의에는 최소 다음 항목을 별도 제목으로 표시한다.

1. 수집하는 개인정보 항목
2. 수집·이용 목적
3. 보유·이용 기간
4. 동의를 거부할 권리
5. 동의 거부 시 제한되는 서비스

- 필수 동의와 선택 동의를 하나의 체크박스로 묶지 않는다.
- 마케팅 수신 동의를 추가할 경우 개인정보 수집·이용 동의와 별도 필드·별도 버전으로 만든다.
- 동의 문구 변경은 설문 질문 변경과 별개로 버전 이력을 남긴다.
- 과거 응답에는 당시 동의 전문과 버전을 보관한다.
- “동의하지 않음”을 선택한 고객의 식별정보는 저장하지 않는다.

#### 초기 수집 항목

| 분류 | 항목 | 초기 필수 여부 | 목적 |
|---|---|:---:|---|
| 식별 | 성함 | 필수 | 고객 확인 및 상담 |
| 연락 | 휴대전화번호 | 필수 | 고객 식별 및 상담 연결 |
| 인구통계 | 성별 | 선택 | 상담 참고·통계 |
| 인구통계 | 생년월일 | 선택 | 상담 참고·연령대 통계 |
| 위치 | 주소 | 선택 | 방문권역 통계 |
| 상담 | 방문동기, 관심 메뉴, 이미지, 모발·두피 고민 등 | 일부 필수 | 상담 준비 및 서비스 개선 |
| 운영 | 매장, 담당 디자이너, 제출 시각 | 시스템 생성 | 권한 관리·통계·감사 |
| 동의 증적 | 문구 버전, 동의 시각, IP, 사용자 에이전트 | 필수 | 동의 사실 확인 |

#### 정책 확정 전 필수 결정

| ID | 결정 항목 | 구현 기준 |
|---|---|---|
| `POLICY-TBD-01` | 고객·응답 보유기간 및 파기 주기 | 일수/개월 수 설정값, 미설정 시 운영 배포 차단 |
| `POLICY-TBD-02` | 백업 주기·보존기간 | 스케줄·보존일 설정값 |
| `POLICY-TBD-03` | XLSX 파일 서버 보존시간 | 시간 단위 설정, 제안 24시간 |
| `POLICY-TBD-04` | 소수 집계 노출 기준 | 최소 건수 설정, 제안 5건 |
| `POLICY-TBD-05` | 만 14세 미만 고객 처리 | 법정대리인 동의 수집 또는 설문 중단 |
| `POLICY-TBD-06` | 주소 수집 수준 | 전체 주소/시·구·동/미수집 중 결정 |
| `POLICY-TBD-07` | 개인정보 처리 위탁·국외 이전 | 인프라·메일·모니터링 사업자 확정 후 문구 반영 |
| `POLICY-TBD-08` | 매장이벤트·할인 안내 운영 | 사용 여부, 발송 채널, 별도 선택 동의 문구와 보유기간 확정 |

### 16.2 데이터 보호

- 모든 외부 통신은 TLS 1.2 이상, 운영은 HTTPS만 허용한다.
- DB와 백업은 저장 시 암호화한다.
- 성함, 연락처, 생년월일, 주소, 법정대리인 정보는 애플리케이션 수준 필드 암호화 또는 동등한 보호를 적용한다.
- 검색용 연락처는 정규화한 뒤 키드 HMAC 인덱스를 별도로 만들어 평문 검색 인덱스를 피한다.
- 비밀번호는 Argon2id 또는 현재 보안 기준에 부합하는 단방향 해시를 사용한다.
- 접근 토큰·세션 ID를 URL, 로그, 분석 이벤트에 남기지 않는다.
- 운영 로그에는 요청 본문, 고객 입력값, XLSX 내용, 비밀번호, 토큰을 기록하지 않는다.
- 환경별 운영·스테이징·개발 DB와 암호화 키를 분리한다.
- 개발·스테이징에는 운영 개인정보를 복제하지 않고 합성 데이터를 사용한다.

### 16.3 세션·인증

- 관리자 세션 쿠키: `Secure`, `HttpOnly`, `SameSite=Lax` 이상.
- 관리자 비활동 만료 30분, 절대 만료 12시간.
- 비밀번호 변경·계정 정지 시 활성 세션을 철회한다.
- 백업 복구, 개인정보 파기, 전체 개인정보 XLSX는 10분 이내 재인증을 요구한다.
- 로그인·초대·비밀번호 재설정에 속도 제한을 적용한다.
- CSRF 보호, 콘텐츠 보안 정책(CSP), 클릭재킹 방지 헤더를 적용한다.

### 16.4 감사·추적

- 감사 로그는 append-only 저장소 또는 수정 불가능한 정책을 사용한다.
- 모든 중요 로그에 `request_id`를 포함한다.
- 로그 시각은 UTC로 저장하고 화면에서 KST로 표시한다.
- 개인정보 전체 열람은 성공 여부와 열람 필드 범위를 남긴다.
- 권한 변경 전후 값은 역할·매장·상태 코드만 기록하고 비밀번호나 토큰은 기록하지 않는다.

### 16.5 파기

- 파기는 정책에 따라 배치 작업으로 실행하고 작업 상태를 추적한다.
- 고객 식별정보, 자유 입력 답변, 동의 증적의 보존 필요성은 법무 검토 결과에 따라 필드별로 설정한다.
- 통계용으로 남기는 데이터는 개인을 다시 식별할 수 없도록 고객 ID와 희소 조합을 제거하거나 재발급한다.
- 백업에 남은 파기 대상 데이터는 백업 보존기간 만료 시 함께 제거되고 그 기간을 파기 화면에 고지한다.

---

## 17. 예외 처리 기준

### 17.1 HTTP·애플리케이션 오류

| 상황 | 코드 예시 | 사용자 메시지 | 처리 |
|---|---|---|---|
| 인증 만료 | `401 SESSION_EXPIRED` | 로그인이 만료되었습니다. 다시 로그인해 주세요. | 현재 관리자 경로 보관 후 로그인 이동 |
| 권한 없음 | `403 FORBIDDEN` | 이 작업을 수행할 권한이 없습니다. | 데이터 미노출, 감사 로그 |
| 리소스 없음/권한 밖 ID | `404 NOT_FOUND` | 요청한 정보를 찾을 수 없습니다. | 목록 이동 |
| 편집 충돌 | `409 EDIT_CONFLICT` | 다른 관리자가 먼저 변경했습니다. | 최신본 비교·다시 불러오기 |
| 중복 제출 | `409 DUPLICATE_SUBMISSION` | 이미 제출이 완료되었습니다. | 기존 성공 확인 후 완료 이동 |
| 검증 실패 | `422 VALIDATION_ERROR` | 입력 내용을 확인해 주세요. | 필드별 오류 연결 |
| 요청 과다 | `429 RATE_LIMITED` | 요청이 많습니다. 잠시 후 다시 시도해 주세요. | 재시도 가능 시각 표시 |
| 서버 오류 | `500 INTERNAL_ERROR` | 처리하지 못했습니다. 오류 ID로 문의해 주세요. | 입력 유지, 오류 ID |
| 복구 중 | `503 MAINTENANCE_MODE` | 시스템 복구 작업 중입니다. | 읽기 전용/상태 갱신 |

#### 재시도

- GET 요청은 네트워크 오류·502·503에 한해 지수 백오프로 최대 2회 자동 재시도한다.
- POST/PUT/PATCH는 `Idempotency-Key`가 있는 경우에만 안전하게 재시도한다.
- 유효성 오류, 권한 오류, 결제·파기·복구 같은 파괴 작업은 자동 재시도하지 않는다.
- 재시도 지연 예시: 1초 → 3초. 429는 `Retry-After`를 우선한다.

### 17.2 고객 설문 예외

| 예외 | 처리 |
|---|---|
| 네트워크 단절 | 입력을 현재 탭 메모리에 유지하고 상단 배너 표시. 재연결 후 임시 저장. |
| 페이지 새로고침 | 유효한 draft 세션이면 서버 저장본 복구. 저장되지 않은 최근 입력은 유실될 수 있음을 입력 중 저장 상태로 최소화. |
| 태블릿 절전 후 복귀 | 토큰·draft 만료 검사 후 복구 또는 직원 호출. |
| 키오스크 토큰 만료 | 고객 입력을 표시하지 않고 직원 호출 화면. |
| 담당 디자이너 정지 | 새 설문 시작 차단. 이미 작성 중이면 제출 전 서버 검증 후 원장이 재배정하도록 직원 화면 전환. |
| 게시 버전 변경 | 진행 중 draft는 기존 버전 유지. |
| 같은 제출 버튼 반복 | 버튼 비활성+멱등 키로 응답 1건만 생성. |
| 연락처 중복 | 고객 화면에는 기존 고객 존재를 알리지 않는다. 서버에서 기존 고객 연결 또는 검토 상태 생성. |
| 브라우저 뒤로가기 | 제출 전 단계 이동 허용, 제출 후에는 완료 화면으로 history replace. |
| 동의 철회 | 작성 중 즉시 draft 식별정보 제거 후 초기화. 제출 후에는 관리자 파기 요청 절차 안내. |

### 17.3 관리자 예외

| 예외 | 처리 |
|---|---|
| 필터 중 사용자 권한 축소 | 다음 API 요청에서 403/404 처리하고 허용 기본 범위로 필터 초기화. |
| 디자이너 매장 이동 | 과거 매장 데이터 접근 종료, 원장·본사 데이터 유지. |
| 매장 비활성화 | 새 설문·초대 차단, 과거 데이터 읽기 유지. |
| 설문 필드 삭제 | 새 버전에서만 비노출, 과거 응답·코드북 유지. |
| 선택지 라벨 변경 | 새 버전에만 반영, 과거 응답은 당시 라벨 유지. |
| XLSX 생성 중 권한 변경 | 다운로드 시 재검사 후 거부하고 파일 만료 처리 가능. |
| 백업 저장공간 부족 | 작업 시작 전 예상 크기 검사, 현재 운영 데이터에는 영향 없이 실패. |
| 복구 실패 | 읽기 전용 유지, 직전 백업 자동 롤백, 오류 ID 제공. |
| 마지막 슈퍼관리자 정지 | 서버에서 409로 차단. |

### 17.4 빈 값·불완전 데이터

- 레거시 또는 복구 데이터에 현재 코드북에 없는 옵션이 있으면 `알 수 없는 값 ({option_code})`으로 표시하고 원본 코드는 유지한다.
- 삭제된 사용자·매장은 `삭제된 사용자`, `비활성 매장`으로 대체하지 않고 제출 당시 스냅샷명 뒤에 상태를 붙인다.
- 날짜 파싱 실패 데이터는 임의 보정하지 않고 `날짜 확인 필요` 배지와 데이터 품질 로그를 생성한다.
- 차트 집계 오류 시 전체 대시보드를 실패시키지 않고 해당 카드만 오류 상태로 표시한다.

---

## 18. 데이터 모델 및 관계

### 18.1 핵심 엔터티

| 엔터티 | 필수 주요 필드 |
|---|---|
| `organizations` | `id`, `name`, `status`, `created_at` |
| `stores` | `id`, `organization_id`, `code`, `name`, `status`, `address`, `phone` |
| `users` | `id`, `organization_id`, `store_id`, `role`, `status`, `name`, `email`, `password_hash` |
| `kiosk_sessions` | `id`, `store_id`, `designer_user_id`, `token_hash`, `expires_at`, `ended_at` |
| `survey_templates` | `id`, `organization_id`, `name`, `active_version_id` |
| `survey_versions` | `id`, `template_id`, `version_number`, `status`, `revision`, `published_at`, `published_by` |
| `survey_fields` | `id`, `version_id`, `section_id`, `field_key`, `field_type`, `label`, `required`, `sort_order`, `config_json` |
| `survey_options` | `id`, `field_id`, `option_code`, `label`, `sort_order`, `exclusive` |
| `consent_text_versions` | `id`, `type`, `version`, `content`, `effective_at` |
| `customers` | `id`, `store_id`, 암호화 기본정보, `phone_hmac`, `status`, `first_response_at`, `last_response_at` |
| `survey_responses` | `id`, `customer_id`, `store_id`, `designer_user_id`, `version_id`, 스냅샷, `status`, `submitted_at` |
| `survey_answers` | `id`, `response_id`, `field_key`, 필드 스냅샷, `value_json` |
| `consent_records` | `id`, `response_id`, `text_version_id`, `subject_type`, `agreed`, `agreed_at`, 증적 메타데이터 |
| `export_jobs` | `id`, `requested_by`, 필터, 개인정보 수준, `status`, `expires_at` |
| `backup_jobs` | `id`, `type`, `status`, `schema_version`, `checksum`, `expires_at` |
| `audit_logs` | `id`, `actor_id`, `action`, `target_type`, `target_id`, `store_id`, `result`, `request_id`, `created_at` |

### 18.2 관계·무결성

- 매장 1 : N 사용자, 고객, 응답.
- 고객 1 : N 응답.
- 설문 템플릿 1 : N 버전.
- 설문 버전 1 : N 필드, 응답.
- 응답 1 : N 답변, 동의 기록.
- 제출된 응답의 `store_id`, `designer_user_id`, `version_id`는 변경하지 않는다.
- 고객 정정이 과거 응답 스냅샷을 덮어쓰지 않는다.
- 게시 버전이나 응답이 참조하는 레코드는 물리 삭제 대신 상태 변경 또는 보존한다.
- 고객·응답 생성은 하나의 DB 트랜잭션으로 처리한다.
- `Idempotency-Key + kiosk_session_id`에 유일 제약을 둔다.
- 같은 매장 `phone_hmac`는 고객 연결 후보 인덱스로 사용하되, 정책상 중복 고객을 허용하므로 절대 유일 제약으로 두지 않는다.

### 18.3 설문 필드 설정 예시

```json
{
  "field_key": "interested_services",
  "field_type": "MULTI_CHOICE",
  "label": "관심 있는 메뉴",
  "help_text": "관심 있는 항목을 모두 선택해 주세요.",
  "required": true,
  "validation": {
    "min_selections": 1,
    "max_selections": null
  },
  "options": [
    {
      "option_code": "CUT",
      "label": "컷",
      "exclusive": false
    },
    {
      "option_code": "DECIDE_AFTER_CONSULTATION",
      "label": "상담 후 선택",
      "exclusive": true
    }
  ]
}
```

---

## 19. 성능·운영 품질 기준

### 19.1 성능 목표

| 항목 | 목표 |
|---|---|
| 고객 설문 첫 화면 LCP | 일반 매장 네트워크에서 2.5초 이내 |
| 고객 단계 전환 | 저장 제외 시 200ms 이내 시각 반응 |
| 관리자 목록 API | 20건 기본 조회 p95 1초 이내 |
| 대시보드 API | 최근 30일, 100개 매장 기준 p95 2초 이내 |
| 검색 | 입력 후 결과 p95 1초 이내 |
| 설문 제출 | p95 2초 이내, 10초 초과 시 상태 확인 흐름 |
| XLSX | 10,000응답 기준 60초 이내 생성 목표 |

- 설문 필수 CSS와 폰트는 우선 로드하고 차트 코드는 관리자에서만 지연 로드한다.
- 고객 화면에는 관리자 번들, Chart.js, XLSX 라이브러리를 포함하지 않는다.
- 목록은 서버 페이지네이션을 사용한다.
- 대시보드 집계는 권한·필터 키 기준 캐시를 사용할 수 있으나 최대 5분 이내 최신성을 유지하고 마지막 갱신 시각을 표시한다.

### 19.2 지원 브라우저

- 최신 Chrome, Edge, Safari의 현재 및 직전 1개 주요 버전.
- iPadOS Safari 최신 및 직전 1개 주요 버전.
- Android Chrome 최신 및 직전 1개 주요 버전.
- Internet Explorer는 지원하지 않는다.
- 태블릿은 최소 768px CSS viewport, 터치 입력을 기준으로 한다.

### 19.3 관측성

- 모든 API 응답에 `request_id`를 제공한다.
- 오류율, 응답시간, 제출 성공률, 임시 저장 실패율, XLSX 실패율, 백업 실패를 모니터링한다.
- 개인정보 값은 오류 추적 도구의 breadcrumb, request body, session replay에서 마스킹한다.
- 제출 실패율이 5분간 5%를 초과하거나 백업이 2회 연속 실패하면 운영 경보를 발생시킨다.

---

## 20. QA 및 인수 기준

### 20.1 고객 설문

- [ ] 360px, 390px, 768px, 820px, 1024px에서 가로 스크롤 없이 모든 문항을 작성할 수 있다.
- [ ] 모든 터치 대상이 최소 44×44px, 고객 주요 입력은 48px 이상이다.
- [ ] 필수 동의 전에는 개인정보 입력과 다음 단계 이동이 불가능하다.
- [ ] 만 14세 미만 분기에서 법정대리인 필드와 동의가 필수다.
- [ ] `지인소개` 선택 시에만 소개자 성함이 표시되고 해제 시 값이 제거된다.
- [ ] 배타 선택지와 일반 선택지가 동시에 저장되지 않는다.
- [ ] 네트워크 단절 후 재연결해도 현재 탭의 입력이 유지되고 저장된다.
- [ ] 제출 버튼을 연속 클릭해도 응답이 1건만 생성된다.
- [ ] 제출 완료 후 뒤로가기로 이전 고객 정보를 볼 수 없다.
- [ ] 완료 화면 15초 후 개인정보가 없는 시작 화면으로 초기화된다.

### 20.2 권한

- [ ] 슈퍼관리자는 전체 매장 데이터를 조회한다.
- [ ] 원장은 소속 매장 외 ID를 URL·API에 넣어도 데이터를 조회하지 못한다.
- [ ] 디자이너는 같은 매장 다른 디자이너의 응답·고객을 조회하지 못한다.
- [ ] 권한 밖 리소스는 존재 여부가 노출되지 않는다.
- [ ] 사용자 매장 이동·정지 후 다음 요청부터 권한이 갱신된다.
- [ ] XLSX 생성과 실제 다운로드 시점에 권한을 각각 검사한다.
- [ ] 개인정보 전체값 열람·내보내기가 감사 로그에 남는다.

### 20.3 설문 편집

- [ ] 필드 추가·삭제·복제·순서 변경을 마우스, 터치, 키보드로 수행할 수 있다.
- [ ] 게시된 버전은 수정할 수 없다.
- [ ] 새 게시 후 기존 진행 중 설문은 이전 버전으로 제출된다.
- [ ] 과거 응답은 당시 질문·선택지 라벨로 표시된다.
- [ ] 잘못된 조건, 중복 키, 선택지 부족, 동의 누락 시 게시를 차단한다.
- [ ] 동시 편집 충돌에서 한 관리자의 변경을 조용히 덮어쓰지 않는다.

### 20.4 목록·대시보드·XLSX

- [ ] 동일 필터에서 KPI 제출 건수, 응답 목록 건수, XLSX 응답 수가 일치한다.
- [ ] 다중 선택 비율의 분모와 설명이 명세와 일치한다.
- [ ] 차트마다 데이터 표와 접근 가능한 이름이 있다.
- [ ] 필터 결과 0건과 시스템 전체 0건의 빈 상태가 구분된다.
- [ ] XLSX의 사용자 입력이 Excel 수식으로 실행되지 않는다.
- [ ] 개인정보 포함 수준과 역할별 제한이 적용된다.
- [ ] 50,000건 초과 작업이 백그라운드로 전환된다.

### 20.5 백업·복구

- [ ] 백업 체크섬과 레코드 수가 기록된다.
- [ ] 복구 전 백업 실패 시 복구가 시작되지 않는다.
- [ ] 복구 중 쓰기 API가 차단된다.
- [ ] 복구 성공 후 핵심 엔터티 수를 검증한다.
- [ ] 실패 시 직전 상태로 롤백하고 개인정보 없는 오류 보고서를 생성한다.
- [ ] 복구 생성·실행·실패·완료가 감사 로그에 남는다.

### 20.6 접근성

- [ ] 키보드만으로 로그인, 설문 작성, 필터, 표 정렬, 모달, 설문 필드 정렬을 수행할 수 있다.
- [ ] 포커스 표시가 모든 배경에서 명확하다.
- [ ] 200% 확대에서도 핵심 기능과 콘텐츠가 잘리지 않는다.
- [ ] 스크린리더가 필드 라벨, 필수 여부, 오류, 현재 단계, 저장·제출 결과를 읽는다.
- [ ] 색상 제거 또는 색각 시뮬레이션에서도 선택·상태를 구분할 수 있다.
- [ ] `prefers-reduced-motion`에서 불필요한 애니메이션이 제거된다.
- [ ] 자동 접근성 검사와 수동 키보드·스크린리더 검사를 모두 수행한다.

### 20.7 보안

- [ ] IDOR, 역할 상승, 매장 범위 우회 테스트를 통과한다.
- [ ] 고객 입력·토큰·비밀번호가 애플리케이션 로그에 남지 않는다.
- [ ] CSRF, XSS, SQL injection 기본 보안 테스트를 통과한다.
- [ ] 관리자 쿠키 보안 속성이 적용된다.
- [ ] 개인정보 필드와 백업이 암호화된다.
- [ ] 운영 데이터를 개발·스테이징으로 복사하지 않는다.

---

## 21. 개발·디자인 산출물 기준

### 21.1 디자인

- Figma 페이지를 `00 Foundations`, `01 Components`, `02 Customer Survey`, `03 Admin`, `04 Responsive`, `05 Prototype` 순서로 구성한다.
- 색상·타입·간격을 변수로 등록하고 본문 토큰명과 동일하게 맞춘다.
- 모든 공통 컴포넌트에 상태 Variant를 만든다.
- 고객 화면은 390, 820, 1280 기준 프레임을 제공한다.
- 관리자 화면은 390, 1024, 1440 기준 프레임을 제공한다.
- 파괴적 작업, 오류, 로딩, 빈 상태를 정상 화면과 함께 디자인한다.

### 21.2 프론트엔드

- 공통 컴포넌트를 문서화 도구(예: Storybook)에서 독립 검증한다.
- 컴포넌트 상태명과 도메인 enum은 본 문서와 동일하게 사용한다.
- 폼 스키마에서 고객 설문을 렌더링하고 필드별 하드코딩을 최소화한다.
- 단, 개인정보 동의·검토·완료·키오스크 종료는 보안 흐름이므로 일반 질문 필드와 분리된 시스템 컴포넌트로 구현한다.
- 디자인 토큰은 CSS 변수 또는 테마 객체로 한 곳에서 관리한다.

### 21.3 백엔드

- 역할·매장·디자이너 범위를 공통 정책 계층에서 강제한다.
- 설문 제출, 고객 연결, 동의 저장은 하나의 트랜잭션으로 처리한다.
- 모든 목록은 서버 페이지네이션, 정렬 허용 목록, 필터 스키마 검증을 적용한다.
- 버전·상태 전이는 서버에서 허용 가능한 방향만 받는다.
- XLSX·백업·파기는 작업 큐와 상태 추적을 사용한다.

### 21.4 QA

- 정상 흐름뿐 아니라 권한 밖 ID 직접 접근, 네트워크 중단, 중복 클릭, 동시 편집, 복구 실패를 포함한다.
- 종이 설문 원본과 초기 디지털 필드·선택지를 게시 전에 대조한다.
- 대시보드 원본 쿼리, 목록, XLSX를 같은 테스트 데이터로 교차 검증한다.

---

## 22. 출시 전 필수 결정 및 체크리스트

### 22.1 비즈니스·법무 결정

- [ ] 개인정보 보유·이용 기간
- [ ] 고객 요청에 따른 정정·파기 접수 책임자와 처리 기한
- [ ] 만 14세 미만 고객의 법정대리인 동의 운영 여부
- [ ] 주소 수집 범위와 필수 여부
- [ ] 매장이벤트·할인 안내 유지 여부와 별도 마케팅 수신 동의·발송 채널
- [ ] 처리 위탁사·국외 이전 여부와 동의/고지 문구
- [ ] 백업 및 XLSX 보존기간
- [ ] 소수 집계 노출 최소 건수

### 22.2 브랜드 결정

- [ ] 공식 idHAIR 로고 SVG 제공
- [ ] 공식 디지털 브랜드 색상과 사용 규정 확인
- [ ] 매장·직급 공식 표기 확인

### 22.3 운영 결정

- [ ] 최초 100개 매장 코드 목록
- [ ] 슈퍼관리자 최소 2인 지정
- [ ] 매장·사용자 초기 등록 방식
- [ ] 장애·개인정보 문의 연락처
- [ ] 백업 복구 승인자와 월간 복구 테스트 일정

---

## 23. 참조 기준

- 개인정보 수집·이용의 법적 근거와 고지 항목은 대한민국 개인정보 보호법 및 실제 운영 시점의 최신 법령·가이드를 법무 검토 후 확정한다.
- 접근성은 [W3C Web Content Accessibility Guidelines 2.2](https://www.w3.org/TR/WCAG22/)를 기준으로 한다.
- Chart.js `canvas`에는 대체 텍스트·ARIA 또는 HTML 데이터 표가 별도로 필요하므로 [Chart.js Accessibility](https://www.chartjs.org/docs/latest/general/accessibility.html)를 적용한다.
- 개인정보 동의 화면의 항목·목적·기간·거부권 구조는 [국가법령정보센터 개인정보 수집·이용 동의서 예시](https://law.go.kr/LSW/flDownload.do?bylClsCd=200203&flNm=%5B%EB%B3%84%EC%A7%80+9%5D+%EA%B0%9C%EC%9D%B8%EC%A0%95%EB%B3%B4+%EC%88%98%EC%A7%91%C2%B7%EC%9D%B4%EC%9A%A9+%EB%8F%99%EC%9D%98%EC%84%9C%28%EC%84%9C%EC%8B%9D%29&flSeq=144642827)의 구조를 참고하되, 아이디헤어 실제 목적과 정책에 맞게 최종 문구를 작성한다.

---

## 부록 A. 디자인 토큰 예시

```css
:root {
  --color-brand-950: #171717;
  --color-brand-800: #292524;
  --color-accent-700: #842e4b;
  --color-accent-600: #a23b5c;
  --color-accent-100: #f9dde7;
  --color-accent-50: #fdf2f6;
  --color-neutral-950: #0a0a0a;
  --color-neutral-700: #404040;
  --color-neutral-600: #525252;
  --color-neutral-400: #a3a3a3;
  --color-neutral-200: #e5e5e5;
  --color-neutral-100: #f5f5f5;
  --color-neutral-50: #fafafa;
  --color-white: #ffffff;
  --color-success-700: #16794a;
  --color-warning-700: #9a6700;
  --color-error-700: #b42318;
  --color-info-700: #175cd3;
  --color-focus: #2563eb;

  --space-1: 4px;
  --space-2: 8px;
  --space-3: 12px;
  --space-4: 16px;
  --space-5: 20px;
  --space-6: 24px;
  --space-8: 32px;
  --space-10: 40px;
  --space-12: 48px;
  --space-16: 64px;

  --radius-sm: 6px;
  --radius-md: 10px;
  --radius-lg: 14px;
  --radius-xl: 20px;
  --shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.06);
  --shadow-md: 0 8px 24px rgba(0, 0, 0, 0.1);
  --shadow-lg: 0 20px 48px rgba(0, 0, 0, 0.16);
}
```

## 부록 B. 초기 설문 단계 배치

| 단계 | 포함 필드 |
|---|---|
| 동의 | `age_14_or_over`, `privacy_consent`, 조건부 법정대리인 필드 |
| 1. 기본 정보 | `customer_name`, `gender`, `birth_date`, `phone`, `address` |
| 2. 방문·상담 정보 | `visit_source`, `introducer_name`, `style_photo_plan`, `preferred_designer_level`, `interested_services`, `desired_image`, `priority_points` |
| 3. 모발·두피 정보 | `scalp_concerns`, `hair_concerns`, `homecare_purchase_history` |
| 4. 검토 | 모든 입력의 읽기 전용 요약과 섹션별 수정 |

## 부록 C. 상태 전이

```text
설문 버전:
DRAFT ──게시──> PUBLISHED ──새 버전 게시──> ARCHIVED

응답:
DRAFT ──제출──> SUBMITTED ──무효 처리──> VOIDED
                         └─파기 요청──> DESTRUCTION_PENDING ──승인·실행──> DESTROYED
VOIDED ──무효 해제──> SUBMITTED

사용자:
INVITED ──초대 수락──> ACTIVE ──정지──> SUSPENDED
INVITED ──72시간 경과──> INVITATION_EXPIRED
SUSPENDED ──재활성화──> ACTIVE
```
