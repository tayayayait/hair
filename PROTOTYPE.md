# 아이디헤어 신규 고객 설문 프로토타입

## 1. 목표와 범위

이 프로토타입은 지원·발표 자리에서 서비스 방향성과 핵심 가치를 3~5분 안에 보여주는 것을 목표로 한다. 전체 운영 시스템을 만들기보다 아래 한 줄 흐름을 실제로 연결한다.

> 관리자가 고객 설문을 준비한다 → 고객이 설문을 제출한다 → 관리자가 결과를 확인한다.

| 분류 | 범위 |
| --- | --- |
| 실제 동작 | 데모 관리자 로그인, 키오스크 세션, 고객 설문 6단계, 검증·중복 방지, 실제 DB 제출, 대시보드, 응답 목록·상세, 개인정보 마스킹 |
| 방향만 표현 | 고정 설문 버전, 다매장·권한 확장 구조, 향후 내보내기 진입점 |
| 정식 개발로 이관 | 전체 RBAC, 동적 설문 편집기, 고객 통합, XLSX 작업 큐, 백업·복구, 보존기간 기반 파기, 전체 감사 로그, CRM 연동 |

프로토타입에는 실제 고객 개인정보를 입력하지 않는다. 발표와 테스트에는 아래 합성 데이터만 사용한다.

## 2. 데모 데이터

### 매장·디자이너

- 아이디헤어 강남점: 김지훈 실장, 이서연 선임수석실장, 박도현 부원장
- 아이디헤어 홍대점: 최유진 실장, 정민석 원장, 한소희 실장

### 샘플 고객

| 항목 | 입력값 |
| --- | --- |
| 성함 | 테스트고객 |
| 성별 | 응답하지 않음 |
| 생년월일 | 1995-05-15 |
| 연락처 | 010-0000-0000 |
| 주소 | 서울시 강남구 테스트동 |
| 방문동기 | 네이버 검색 |
| 관심 메뉴 | 컷, 모발케어 |
| 원하는 이미지 | 자연스러운, 세련된 |
| 중요 포인트 | 손질이 편한 |

## 3. 최종 PHASE 구성

### PHASE 0 — 기준선과 시연 시나리오

- 의존성 설치와 실행 환경 확정
- 린트·타입·테스트·빌드 기준선 구성
- 합성 데모 데이터와 3~5분 발표 흐름 확정
- 전 화면 프로토타입 안내와 개인정보 입력 금지 표시

완료 기준: `npm run check`를 반복 실행할 수 있고, 시연 데이터와 흐름이 문서화되어 있다.

### PHASE 1 — 고객 설문 핵심 흐름

- 동의, 기본 정보, 방문·상담, 모발·두피, 검토, 완료의 6단계 구성
- 단계별 필수값, 형식, 조건부 문항 검증
- 독점 선택지와 조건 변경 시 숨겨진 값 제거
- 제출 중 중복 클릭 방지와 완료 후 로컬 개인정보 초기화

완료 기준: 잘못된 입력을 차단하고, 한 번의 정상 제출만 허용하며, 완료 후 이전 고객 정보가 남지 않는다.

### PHASE 2 — 안전한 제출 경계

- 추측하기 어려운 키오스크 토큰과 만료 세션
- 서버 RPC에서 토큰·매장·디자이너·설문 버전·payload 재검증
- idempotency key로 중복 제출 방지
- 익명 사용자의 테이블 직접 쓰기 제거
- 활성 데모 관리자만 응답을 읽는 RLS 정책

완료 기준: 클라이언트가 임의의 매장이나 설문 버전으로 데이터를 직접 저장할 수 없고, 동일 제출 재시도는 같은 결과를 반환한다.

### PHASE 3 — 관리자 로그인과 키오스크 준비

- Supabase Auth 기반 데모 관리자 로그인
- 관리자 보호 라우트와 비활성 계정 차단
- 매장·디자이너 선택, 고객 링크 생성, 새 창 열기
- 만료·종료·잘못된 토큰의 고객 안내 화면

완료 기준: 인증된 데모 관리자만 고객 설문 링크를 만들 수 있고, 유효하지 않은 고객 링크는 설문을 열지 않는다.

### PHASE 4 — 결과 확인 화면

- 전체·오늘·응답 매장·최다 관심 시술 KPI
- 매장별 막대 차트와 관심 시술 원형 차트
- 날짜·매장·디자이너 필터와 최근 응답
- 응답 목록 검색, 모바일 카드/데스크톱 표
- 기본 마스킹 상세와 명시적 개인정보 보기 전환

완료 기준: 방금 제출한 합성 응답을 검색해 상세 답변과 버전·동의 정보를 확인할 수 있다.

### PHASE 5 — 발표 품질

- 390/768/1024/1440px 반응형 확인
- 키보드 포커스, 레이블, 본문 건너뛰기, 오류 요약
- 로딩·빈 결과·오류·만료 상태
- 프로토타입 안내 배너와 일관된 브랜드 표현
- reduced-motion 대응과 가로 스크롤 방지

완료 기준: 주요 고객·관리자 화면이 모바일과 데스크톱에서 깨지지 않고, 기본 키보드 탐색이 가능하다.

### PHASE 6 — 원격 활성화와 제출 링크

- 대상 Supabase 프로젝트에 마이그레이션 적용
- Supabase Auth에 데모 관리자 생성 후 `admin_profiles` 연결
- 합성 데이터로 전체 시나리오 QA
- 호스팅 환경 변수 설정과 프로덕션 배포
- HTTPS 접속 링크, 데모 계정 전달, 발표 직전 데이터 초기화

완료 기준: 새 브라우저에서 배포 링크로 로그인부터 제출 결과 확인까지 끊김 없이 재현된다.

## 4. 현재 구현 상태

| PHASE | 상태 | 비고 |
| --- | --- | --- |
| 0 | 완료 | 실행·품질 기준선과 시연 데이터 확정 |
| 1 | 완료 | 설문 검증·조건부 값·중복 제출·초기화 구현 |
| 2 | 로컬 완료 | 마이그레이션 작성·정적 계약 테스트 완료, 대상 Supabase 적용 필요 |
| 3 | 로컬 완료 | 인증·키오스크 UI 완료, 원격 데모 계정 연결 필요 |
| 4 | 완료 | 대시보드·목록·상세·마스킹 구현 |
| 5 | 완료 | 390~1440px 브라우저 QA와 접근성 보완 완료 |
| 6 | 진행 필요 | 원격 DB 적용, 실데이터 QA, 배포 링크 발급 |

## 5. 원격 활성화 순서

### 5.1 Supabase

대상 프로젝트 ID는 `ngkfvnxhpcboiaksuzou`이다. 프로젝트 소유자 권한이 있는 환경에서 다음 마이그레이션을 순서대로 적용한다.

1. `supabase/migrations/20260803040303_27250f1b-6b73-4394-8eed-ba9b57ae2100.sql`
2. `supabase/migrations/20260803040325_45a7a33c-3561-4061-a20d-7a5aae114826.sql`
3. `supabase/migrations/20260803045922_prototype_security_boundary.sql`

이미 앞의 두 파일이 적용된 프로젝트라면 세 번째 보안 경계 마이그레이션만 새로 적용한다. 마이그레이션 후 Supabase Auth에서 데모 이메일 사용자를 만들고, 생성된 사용자 UUID로 아래 프로필을 연결한다.

```sql
insert into public.admin_profiles (user_id, display_name, role, is_active)
values ('<AUTH_USER_UUID>', '데모 관리자', 'DEMO_ADMIN', true)
on conflict (user_id) do update
set display_name = excluded.display_name,
    role = excluded.role,
    is_active = excluded.is_active;
```

발표 직전 응답 데이터만 비우려면 `supabase/scripts/reset_prototype_data.sql`을 권한 있는 SQL 편집기에서 실행한다.

### 5.2 호스팅

배포 환경에 아래 공개용 Supabase 값을 설정하고 프로덕션 빌드를 배포한다. 서비스 역할 키, DB 비밀번호, 관리자 비밀번호는 클라이언트 환경 변수나 저장소에 넣지 않는다.

```text
VITE_SUPABASE_PROJECT_ID=ngkfvnxhpcboiaksuzou
VITE_SUPABASE_URL=https://ngkfvnxhpcboiaksuzou.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=<PUBLISHABLE_KEY>
```

배포 후 `/`, `/login`, `/admin/kiosk`, 발급된 `/s/:session`, `/admin/dashboard`, `/admin/responses`를 새 브라우저에서 순서대로 확인한다.

## 6. 발표 시연 순서

1. `/login`에서 데모 관리자 계정으로 로그인한다.
2. `/admin/kiosk`에서 강남점과 김지훈 실장을 선택한다.
3. 고객 설문을 새 창으로 열고 샘플 고객 정보를 입력한다.
4. 검토 화면에서 답변을 확인하고 한 번 제출한다.
5. 완료 화면에서 이전 고객 정보가 제거되었음을 확인한다.
6. `/admin/dashboard`에서 제출 건수와 최근 응답 반영을 확인한다.
7. `/admin/responses`에서 `테스트고객`을 검색한다.
8. 상세 화면의 기본 마스킹을 보여준 뒤 `개인정보 보기`를 눌러 전체 값을 확인한다.

## 7. 발표 직전 체크리스트

- 배포 링크가 HTTPS로 열리는가
- 데모 관리자 로그인이 가능한가
- 강남점·김지훈 실장으로 키오스크 링크가 생성되는가
- 고객 설문 제출이 한 번만 저장되는가
- 대시보드와 응답 목록에 즉시 반영되는가
- 기본 화면에서 이름·전화번호·생년월일·주소가 마스킹되는가
- 실제 개인정보가 아닌 합성 데이터만 남아 있는가
- 발표용 계정과 링크를 비공개 전달했는가
